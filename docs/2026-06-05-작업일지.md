# 2026-06-05 작업일지

## 선샤인 웰니스 — 문의/상담 UX 개편 + 다크 투어리즘 장소정보 + 교육자료 허브 확장

> 모든 변경은 2026-06-05 작업. 캐시 버스팅: `script.js?v=20260605c`, `style.css?v=20260605c`.
> ⚠️ Vercel은 **커밋 메시지에 "배포"가 있을 때만 빌드**(Ignored Build Step). 없으면 `Canceled` → `vercel --prod --yes` 강제 배포.

---

## 1. 챗봇·문의(Inquiry) UX 개편 — 3단계

### 배경 (현황 파악에서 발견)
- 홈 카드 `문의하기`는 `openProgramsPage()`(상품 목록 이동), 상품페이지 카드 `문의하기`는 `openContactPage()`(문의 폼) — **같은 라벨인데 동작이 달랐음**.
- "여행 정보" 모달이 일부 환경에서 안 열림 → 원인: `openModal`이 Blob + `window.open('_blank')` **새 탭**으로 열어, 모바일/팝업차단에서 무반응.
- 히어로 슬라이더 우측 화살표(`.next`)가 우하단 챗봇 버튼과 겹침.

### Step 1 — '여행 정보' 모달: 새 탭 → 인페이지 iframe 오버레이 (`116b4e9`)
- `openModal`에서 `Blob`+`window.open` 제거. 죽어있던 `#modalOverlay`/`#modalContent`를 재사용.
- 생성된 **완전한 HTML 문서를 `<iframe srcdoc>`로 격리**해 인페이지 오버레이에 표시 → 스타일 충돌 없음, 모바일·팝업차단에서도 항상 열림.
- `closeProgramModal()` 추가: ✕ 버튼 / 배경 클릭 / ESC 로 닫기, body 스크롤 잠금. 오버레이 없을 때만 기존 새 탭 방식 폴백.
- CSS: `.modal-box.modal-box-frame`(94%/max 860px/90vh, 패딩0), `.modal-frame`(iframe 풀필), 닫기버튼 흰 원형.

### Step 2+3 — '문의하기' → AI 상담 팝업으로 통일 (`4983593`)
- 홈 6개 + 상품 9개 카드의 `문의하기`를 **모두 `openConsult(programKey)`** 로 교체 → 홈/상품 동작 통일(3번)이 2번에 흡수됨.
- `openConsult()`: 챗봇 팝업을 열고, **누른 프로그램명을 첫 메시지로 자동 안내**("「부산 다크 투어리즘」 문의 주셔서 감사합니다…").
- 챗봇 헤더: **"선샤인 AI 상담사" + 💁‍♀️ 아바타**(이모지 임시본 — 추후 일러스트 `<img>` 교체 가능), 상태문구 "실시간 자동 응답 · 빠른 연결".
- 챗봇 상단에 **빠른 연결 버튼 상시 노출**: `[📞 전화]`(tel) · `[💛 카카오]`(오픈채팅) · `[📝 견적문의]`(챗봇 닫고 `openContactPage()`). → AI 즉답 + 실제 견적 리드 유지.
- 푸터·About·고객센터의 "견적의뢰" CTA는 기존 문의 폼 그대로 유지(의도된 구분).
- CSS: `.chatbot-quick` / `.cbq-btn` 추가.
- z-index 확인: `#subPageOverlay`(900) < 챗봇(9000) → 상품 페이지 위에서도 정상 노출.

### 화살표 겹침 수정 (`37036df`)
- 히어로 슬라이더 `.prev`/`.next` `right` 값 증가(데스크톱 56/112→96/152, 모바일 24/80→88/144)로 우하단 챗봇 코너를 비켜남. 챗봇은 우하단(`right:24/16`) 유지.

### 제작물
| 파일 | 내용 |
|---|---|
| `script.js` | `openModal` iframe 오버레이화, `closeProgramModal`, `openConsult`, 상품페이지 카드 버튼 onclick 변경 |
| `index.html` | 홈 6카드 `문의하기`→`openConsult`, 챗봇 헤더/아바타/빠른버튼 추가 |
| `style.css` | `.modal-box-frame`/`.modal-frame`, `.chatbot-quick`/`.cbq-btn`, 화살표 위치 |

---

## 2. 다크 투어리즘 '여행 정보' — 부산근현대역사관 실용정보 추가 (`b0e3faa`)

- 기존 `spotDetails` 항목(이름·주소·설명·💡코멘트·지도)에 **추가**: 운영시간(화~일 09:00–18:00)·휴관(매주 월요일)·전화(051-607-8000)·교통(중앙역 5번 출구 457m)·공식사이트(busan.go.kr/mmch).
- 데이터 필드 신설: `hours` / `closed` / `phone` / `transit` / `officialUrl`. 주소에 "별관" 명시.
- 렌더(`spotDetailsHTML`)에 **값이 있을 때만 표시되는 `.spot-info` 줄** 추가 → 같은 렌더를 쓰는 다른 장소(사찰 투어 등)엔 영향 없음.

---

## 3. 교육자료(/learn) 허브 확장

| 커밋 | 내용 |
|---|---|
| `037707c` | **Module 08 웰니스 관광 · 09 영상 홍보 제작 실습** 신규 추가 (`learn/wellness-tourism.html`, `learn/video-promotion.html`), 허브 카드 2개 + sitemap 등록 |
| `e6e6d2a` | 허브 그리드 2열→**3열(9개=3×3 정사각)** + 반응형 3→2→1열 (`learn/index.html`, `learn/learn.css`) |
| `f46dcd7` | 허브 폭 960→**1160px** (프로그램 페이지와 통일) |

---

## 커밋 이력 (2026-06-05)

| 커밋 | 내용 |
|---|---|
| `37036df` | 히어로 슬라이더 화살표 왼쪽 이동 — 챗봇 겹침 해소 |
| `b0e3faa` | 다크 투어리즘 부산근현대역사관 운영정보 추가 |
| `037707c` | 교육자료 Module 08/09 추가 |
| `e6e6d2a` | 교육자료 허브 그리드 3열 |
| `116b4e9` | 여행 정보 모달 → 인페이지 iframe 오버레이 (Step 1) |
| `f46dcd7` | 교육자료 허브 폭 1160px |
| `4983593` | 문의하기 → AI 상담 팝업 통일 + 상담사 아바타/빠른버튼 (Step 2+3) |

---

## 현재 배포 상태
- 캐시 버스팅: `script.js?v=20260605c`, `style.css?v=20260605c`
- 배포: `https://sunshine-wellness.vercel.app` / 도메인 `sunshinewellness.co.kr`(GitHub Pages) — `/api`는 Vercel에서만 실행
- 전 커밋 라이브 반영 확인 완료

## 미완료 / 다음 작업
- 챗봇 아바타(💁‍♀️ 이모지)를 실제 AI 일러스트 이미지로 교체 검토
- 챗봇 창 `width:370px` 고정 → 아주 좁은 폰(≤390px) 대응(예: `calc(100vw-32px)`) 검토
- 다크 투어리즘 EN/ZH '여행 정보'에도 부산근현대역사관 운영정보 동일 반영(현재 KO만)
- 챗봇 FAQ_DB 항목 보강 / (선택) 진짜 LLM 상담 연동 검토
