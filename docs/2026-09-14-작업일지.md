# 2026-09-14 작업일지

## **수업 전날 — 07장이 한 장에서 한 과목이 되는 중**

커밋 **열넷**(09:56 ~ 13:04). 새 페이지 여섯이 들어온 날이다.
**내일이 9월 15일(화) 3주차 수업이고, 같은 날 18시에 경북 AX 경진대회가 마감된다.**

```
① 07장 여행업 — BSP TOP10 통합본 · 채용 링크판 · ONDA 수익화 모델
② 스마트투어리즘 12곳 지도 아래 — 선정 시기와 재원 구조
③ 평가방식 2안에 인터랙티브 강의계획서
④ 07장에 인바운드 여행시장 18섹션(빨강)
⑤ 안내서 설명 줄이 사라졌다 — 다른 세션이 지웠고, 내 CSS 만 남아 있었다
```

---

## 1. **07장 여행업이 가장 두꺼운 장이 됐다** (`3ceb002` · `df5c67f`)

어제 등록 두 줄에 「어떻게 돈이 도는가」 한 줄을 더했는데, 오늘 넷이 더 붙었다.
지금 07장의 알약 줄은 이렇게 이어진다.

```
① 등록한다        정부24 신청 화면 · 사업계획서 예시 · 임대차계약서
② 얼마가 드나      자본금과 법적 근거 · 관광진흥법 시행령 [별표 1]
③ 돈이 어떻게 도는가  수익모델 공식 판(숫자를 넣으면 다시 계산) · 아웃바운드 PPTX
④ 한 장으로 써 본다   AI 프롬프트 워크북(STEP 0~8 · 120분)
⑤ 남들은 어떻게 버나  BSP TOP10 통합본 · 채용 링크판 · ONDA 수익화 모델
⑥ 받는 쪽은 다르다    인바운드 여행시장 18섹션
```

**한 장 안에서 「차릴 수 있게 → 벌 수 있게 → 견줄 수 있게」로 내려간다.**
07장만으로 여행업 창업 수업 한 과목이 된다.

---

## 2. **12곳 지도 아래 — 「어디」 다음에 「언제 · 얼마로」** (`df5c67f`)

12곳 지도는 **어디에 있는지**만 보여 준다. 그 아래에 선정 시기와 재원 구조를 놓았다.

| 알약 | 무엇 |
|---|---|
| **언제 뽑혔고 누가 돈을 냈나 — 12곳 선정 시기 · 재원 구조 ↗** | 2020 시범 → 2021 확산 → 2022 다지역 → 2023 3년형 흐름, 국비 35억(2023년 이후 최대 45억) + 지방비 1:1 이상, 뒤에 **분석 프롬프트 20** |
| **같은 내용 · 수업용 슬라이드 ⤓ PPTX** | 대본 박스 포함 |

> **설명에 박은 한 줄** — **「정부가 전액 낸 사업이 아닙니다」.**
> 도시 카드의 「왜 만들었나」 칸을 쓸 때 이 구조를 모르면 근거가 헐거워진다.
> 2020년 인천만 예외형이다 — 국비 35억 + 지방비 35억 + **민간현물 18억** = 88억.

---

## 3. **평가방식 2안에 웹판** (`df5c67f`)

인터랙티브 강의계획서는 **논문 리딩형(138편 · 30회차)** 이라 2안이 정확히 그 내용이다.
문서 알약 옆에 흰 알약으로 붙였다.

```css
/* 막힌 곳 — .pcol a.mapl 에 margin-top:auto 가 걸려 있어
   알약을 하나 더하면 나란히 서지 않고 위아래로 쌓인다. */
.syl-box .pcol .pc-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: auto; }
.syl-box .pcol .pc-tags a.mapl { margin-top: 0; }
```

줄을 하나 만들어 `margin-top:auto` 를 **알약에서 줄로 옮겼다.**
표지 소속은 열어 확인했다 — **국립경국대학교 관광경영학과**(9/13 에 고친 그 파일이 아니다).

---

## 4. **인바운드 18섹션 — 아웃바운드 옆에 받는 쪽을** (`df5c67f`)

아웃바운드 PPT 바로 아래에 빨간 알약으로 놓았다.
**「아웃바운드가 내보내는 일이라면, 인바운드는 받는 일입니다」**로 이어 붙여
같은 여행업인데 고객 · 유통 · 경쟁 방식이 어떻게 갈리는지 한자리에서 견주게 했다.

정의 → 시장 세분화 → 유통구조 → 산업 변화(OTA · FIT) → 경쟁전략 → 성과 검증,
열여덟 섹션마다 **도식과 상세 설명이 함께** 있다.

> 제목 하나가 교수자 말투였다 — **「17. 학생에게 던질 핵심 질문」 → 「스스로에게 던질」.**
> 어제 세운 규칙대로 올리기 전에 grep 했고, 이 한 줄이 걸렸다.

---

## 5. **안내서 설명 줄 — 세 번 고치는 동안 다른 세션이 지웠다**

이 건이 오늘 가장 볼 만하다.

```
9/13 15:20  73fcd4a  두 줄 격자로 왼쪽 맞추고 군더더기 삭제   (이 세션)
9/13 15:41  841ec63  배지를 칸 왼쪽에 붙임 · 머리말 「안내서」  (이 세션)
9/14 09:56  3ceb002  syl-fine 줄 자체를 삭제                (다른 세션)
9/14 09:59  df5c67f  격자를 풀어 왼쪽 정렬                   (이 세션 — 이미 없는 것을 고쳤다)
```

사용자가 **「왼쪽 정렬」을 세 번 말했다.** 세 번 다 다르게 고쳤지만
**세 번 다 원하는 모양이 아니었고**, 결국 다른 터미널에서 **줄 자체를 지웠다.**
나는 그 뒤에 CSS 만 고쳤으니 **아무 데도 적용되지 않는 코드를 배포한 셈이다.**

오늘 죽은 CSS **14줄**(`syl-fine` 선택자 일곱)을 걷어냈다.

> **교훈 둘.**
> **① 같은 곳을 세 번 고치게 되면 고치는 방법이 아니라 그 요소가 문제다.**
> 세 번째에는 「이 줄을 뺄까요」를 먼저 물었어야 한다.
> **② 남의 커밋이 들어온 뒤에는 내가 고칠 대상이 아직 있는지부터 본다.**
> `git pull` 은 했지만 **내가 만지려던 마크업이 살아 있는지**는 보지 않았다.

---

## 6. **관광 창업 마인드맵 — 말한 페이지와 그림이 달랐다** (`c846a47`)

사용자가 `ChatGPT Image 2026년 9월 13일 오후 03_58_58.png` 를 **「스마트 관광 가장 위 상단에」** 넣으라 했다.
열어 보니 가운데가 **「관광 창업(관광벤처창업론)」**이고 가지 여덟이 **관광벤처창업론의 파트 01~08** 그대로였다.
다운로드 폴더에 9/13 이미지는 이 한 장뿐이어서 파일을 잘못 고른 것도 아니었다.

**물었다** — 사용자가 **「관광벤처창업론 맨 위」**를 골랐다.

| | |
|---|---|
| 자리 | 히어로(강의실 · 학사일정) 바로 아래, `lead-lines` 첫 문장 **위** |
| 폭 | 본문 폭 전부(`max-width:100%`) — 글자가 작아서 |
| 크게 보기 | 교재 그림 둘이 쓰던 `#imgzoom` 스크립트 선택자에 `.learn-fig.mindmap img` 를 더했다 |
| 용량 | PNG **1,900KB → webp 280KB** (`learn/images/tourism-venture-mindmap.webp`) |

> **교훈 — 「어디에」보다 「무엇이」가 먼저다.** 말한 페이지에 그대로 넣었으면
> 스마트 투어리즘 맨 위에 **다른 과목의 지도**가 걸렸다. 그림은 넣기 전에 연다.

### 그림과 페이지를 대조했다 — **파트 이름 여덟은 모두 같다**

| 파트 | 그림 | 페이지 |
|---|---|---|
| 01 | 낼 것을 먼저 본다 | 같음 |
| 02 | 주차별 실습 목표 | 같음 |
| 03 | 창업의 흐름 (01~06장) | 같음 |
| 04 | 어디에 내는가 | 같음 |
| 05 | 업종별로 들어가기 (07~12장, 12장 로컬크리에이터) | 같음 |
| 06 | 창업하지 않아도 쓰인다 | 같음 |
| 07 | 책 — 교재 두 권 | 같음 |
| 08 | 곁들여 보기 · 링크 | 같음 |

**다르던 것은 하나** — 06장 걸음 순서. 그림은 「문제 → 가치 → **기술 → 도달** → 수익화」,
어제 만든 피라미드는 「③ 도달 · ④ 기술 · ⑤ 지속 가능성」이었다.
그런데 **그 사이 다른 세션이 `938462e` 로 ③ ↔ ④ 를 바꾸고 ⑤ 를 「수익화 모델」로** 고쳐 두어,
대조 시점에 **이미 그림과 같아져 있었다.** 그림을 다시 만들 필요는 없다.

---

## 7. **AI 배리어프리 공고 — 링크를 걸고 나서 본 것** (`52668cf` · `f11897c`)

9/13 일지에 알약 추가만 적혀 있어 **공고 원문에서 확인한 두 가지**를 여기 남긴다.

| 확인 | 내용 |
|---|---|
| 주관 · 마감 | **한국관광공사** · 접수 9/7 ~ **9/16(수) 11:00** — 오전이다 |
| 지원 대상 | **AI 등 혁신기술을 보유한 중소기업 창업 기업(업력 1년 미만~10년 미만)** — 예비창업자는 아니다 |

알약은 붉은색 → **고동색**(`#5c3a1e`), 글은 「기반 · 형 · 부문 · 모집」을 빼
**「AI 지역관광 문제해결 프로젝트 (역사문화) AI 배리어프리 ↗」**, 자간 `-0.6px`.

---

## 8. **낮 (12:25 ~ 13:03) — 카드로 세우고, 군말을 걷고, 주차를 옮겼다**

5절의 「다른 세션」이 이 세션이다(`3ceb002`). 그 뒤 낮에 한 일.

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `a18752a` | 관광빅데이터분석 **관련 링크 아홉 → 2열 카드 그리드** | `.reslist.grid2` 위에 페이지 안 `lcard` 변형만 얹었다. 초록 윗선 · 둥근 테두리 · 출처 줄은 점선 아래 카드 바닥. 760px 아래 한 줄. 아홉이라 캐글이 왼쪽에 혼자 선다 |
| `851968c` | 관광벤처 강의계획서 제목 옆 **「안 1 · 안 2」 배지 삭제** | 옆 워크북 알약은 남김 |
| `430b472` | 같은 상자 **「두 안 모두 기본정보 · 개요 · … 수업규정을 담았습니다」 삭제** | 뒤의 「정규 수업은 14주차(12/1)로 끝납니다」는 남김 |
| `edff8b4` | **워크북 시트 1 → 3주차로**, 시트 2 와 한 「할 것」에 · **2주차 할 것은 벤치마킹 발표** | 아래 |
| `ea7168a` | 벤치마킹 머리줄 끝에 **「9월 8일 진행함」** 작은 회색 글씨(`.lp-when`) | 이미 지난 수업에서 다룬 절임을 알림 |

### 2주차 ↔ 3주차 — 따라 바뀌어야 할 곳이 넷이었다 (`edff8b4`)

요청은 「시트 1을 3주차로 옮기고 그 자리에 벤치마킹 발표」 한 줄이었지만, 옮기면 **말이 어긋나는 곳**이 셋 더 있었다.

| 자리 | 전 | 후 |
|---|---|---|
| 2주차 할 것 | 워크북 시트 1 + 「아직 아이템이 없어도 괜찮습니다 — 고르는 연습」 | **벤치마킹 발표** — THE VC 목록에서 회사 한 곳, 위캔 · 캔슬마켓처럼 문제 → 해결 → 수익 구조 → 성패 요인 → 확장, 수치엔 「공개 기업정보 기준」 |
| 2주차 접힌 제목 꼬리 | … · 워크북 시트 1 | … · **벤치마킹 발표** |
| 3주차 접힌 제목 꼬리 | … · 워크북 시트 2 | … · **워크북 시트 1 · 2** |
| 3주차 안내 | 「**지난주에** 고른 유형이 바뀌어도 괜찮습니다」 | 「**시트 2를 쓰다가 시트 1에서** 고른 유형이 바뀌어도 괜찮습니다」 |

> **교훈 — 내용을 다른 주로 옮기면 「지난주」 「이날은」 같은 시간 낱말이 거짓말을 한다.**
> 옮긴 블록만 보지 말고 **앞뒤 안내 문장과 접힌 제목 꼬리**까지 같이 본다.
> 벤치마킹 발표 문장은 **내가 쓴 것**이다 — 발표 시간 · 인원은 사용자에게 받지 못해 넣지 않았다.

### 같은 파일에 남의 미커밋 수정 — 이번에도 줄 단위로 (`ea7168a`)

`tourism-venture.html` 에 다른 세션의 파트 머리글 작업이 커밋 전으로 있어 `git pull` 조차 막혔다.
9/13 12절 방법(HEAD 본에 내 치환만 → `hash-object` → `update-index`)으로 **3줄만** 커밋했다.
그 사이 올라온 `c94cd63`(파트 머리글 큰 번호 블록)이 **부모로 들어가 있는지**, 내 커밋이 4줄 변경뿐인지 확인했다.

> **주의 — 블롭을 만든 뒤 커밋 전에 남이 커밋하면, HEAD 본 기준 블롭이 그 커밋을 되돌린다.**
> 커밋 직후 `git show HEAD --stat` 의 변경 줄 수가 내 예상과 같은지 반드시 본다.

---

## 9. **파트 머리글을 큰 번호 블록으로** (`c94cd63` · `8e8c8b1`)

사용자가 **「파트 바를 눈에 띄게 하려면 너의 제안은?」**이라 물었다. 세 안을 그려 보였다 —
**꽉 찬 색 바**(가장 강하지만 여덟 번이면 무겁다) · **큰 번호 블록** · **옅은 유령 숫자 + 윗선**(가장 차분).
**큰 번호 블록**을 골랐고, 적용은 **관광벤처창업론만 먼저**로 정했다.

```
┌──────┬──────────────────────────────────────────┐
│ 파트 │  낼 것을 먼저 본다                        │
│  01  │  이 수업의 산출물은 리포트가 아니라…      │
└──────┴──────────────────────────────────────────┘
  56px 진한 색           연한 바탕 + 연한 테두리
```

| | 전 | 후 |
|---|---|---|
| 번호 | 알약 안 11px 「파트 01」 | 색 사각형 안 **26px 숫자**, 위에 9px 「파트」 |
| 테두리 | 왼쪽 6px 선만 | 사방 1px **연한 파트색**(`--pp-ln` 신설) + 모서리 10px |
| 제목 · 머리말 | 가로로 폭을 나눠 가짐 | 오른쪽 칸에 **위아래로** |

**머리말이 눌리지 않게 된 것이 실익이다.** 전에는 제목과 머리말이 한 줄에서 폭을 나눠,
파트 04처럼 머리말이 긴 곳은 글자가 좁게 접혔다.

---

## 10. **그 카드가 바로 깨졌다 — 격자 항목 셋, 칸 둘**

배포 직후 사용자가 화면을 그대로 옮겨 적어 보냈다. 설명이 엉뚱한 자리에 있었다.

```
칸은 둘인데 넣은 항목은 셋 (사각형 · 제목 · 설명)
→ 셋째(.pt-sub)가 자동으로 「둘째 줄 첫 칸」, 즉 색 사각형 아래로 떨어진다
```

`grid-template-columns: 56px 1fr` 만 주고 **각 항목이 어느 칸 어느 줄인지는 적지 않았다.**
CSS 격자는 남는 자리에 알아서 채우므로, 항목 수가 칸 수의 배수가 아니면 이렇게 샌다.

```css
.pt     { grid-column: 1; grid-row: 1 / span 2; }  /* 사각형이 두 줄을 다 차지 */
.pt-ti  { grid-column: 2; grid-row: 1; }
.pt-sub { grid-column: 2; grid-row: 2; }
```

좁은 화면(620px)에서는 한 칸이므로 셋을 1 · 2 · 3행으로 되돌렸다.

> **교훈 — 격자에 항목을 넣을 때는 칸 수와 항목 수를 먼저 센다.**
> 맞지 않으면 `grid-column` · `grid-row` 를 항목마다 못박는다.
> 태그 균형 검사도, 링크 검사도 이건 못 잡는다. **화면을 봐야 보이는 종류다.**

---

## 11. **보증보험 통합본 — 07장의 여섯째 줄** (`8e8c8b1` · `9c89dd0`)

07장 ② 줄이 「자본금 말고 드는 돈 — 등록 수수료 · 등록면허세 · **보증보험** · 사무실 용도」를
말하고 있어, 그중 가장 헷갈리는 칸을 **바로 밑에서 펼치는 자리**에 걸었다.

설명 가운데 세운 것은 한 줄이다.

> **가입금액은 소비자 피해 보상한도이지 내는 돈이 아닙니다. 5,000만원 보증 ≠ 5,000만원 납부.**

학생이 사업계획서 예산 칸에 5,000만원을 적어 넣는 실수가 여기서 나온다.
신규 등록 금액(국내 2,000만 · 국내외 3,000만 · 종합 5,000만, 기획여행 추가 최소 2억)과
「보증보험으로 100% 구제된다」는 오해를 원자료와 현행 법령으로 갈라 둔 점도 적었다.

**2.5MB** 다 — 인포그래픽 둘이 `data:` URI 로 박혀 있다. 외부 이미지가 없어 한 파일로 돈다.
제목 하나가 교수자 말투였다 — **「08 학생에게는 이렇게 설명하면 됩니다」 → 「08 한 문장으로 정리하면」.**

---

## 12. **관광빅데이터분석 히어로를 2열로** (`777194b`)

강의실 바와 학사일정 상자가 위아래로 떨어져 있었다. 둘 다 「언제 · 어디서」를 말하는
정보라 한 줄에 놓으면 세로 공간이 크게 줄고 함께 읽힌다.
본문에 있던 학사일정 상자를 히어로로 올리고, 왼쪽 칸에 강의실 바와 학생 이해 노트
알약 줄을 함께 넣었다. **세 과목 페이지의 머리 모양이 이제 같다.**

> **막힌 곳** — 이 파일이 **CRLF** 였다. `
` 기준으로 줄을 찾다 실패했다.
> 이번 주에 파일마다 줄바꿈이 섞여 있어 두 번째 걸린 건이라,
> 이제 편집 스크립트는 **줄바꿈을 감지해 원래 형식으로 되돌려 쓴다.**

---

## 13. 그 밖에 — **일지 번호가 또 겹쳤다**

낮에 들어온 커밋들(`a18752a` · `851968c` · `430b472` · `edff8b4` · `ea7168a` · `c846a47`)은
**8절에 이미 적혀 있다** — 다른 세션이 같은 시간에 이 일지를 쓰고 있었다.

⚠️ **오늘도 두 세션이 같은 일지 파일에 각자 절을 붙였다.**
9/13 일지에는 **「## 12.」가 둘**이 됐고(12:12 · 12:13), 9/14 일지에는
**6 · 7 · 8** 이 겹쳤다. 이번에는 `stash → pull --rebase → stash pop` 을 써서
**덮이지는 않았고** 양쪽 다 남았다 — 9/13 은 12 · 13 · 14 로, 9/14 는 내 몫을
9 · 10 · 11 · 12 · 13 으로 다시 매겼다.

> **어제 세운 규칙(「작업일지는 쓰기 전에 `git pull`, 덮지 말고 이어 붙인다」)은 지켰다.**
> 덜 지킨 것은 **번호**다. 이어 붙이기 전에 **그 파일의 마지막 절 번호를 먼저 읽어야 한다.**

---

## 14. **관광빅데이터분석 — 데이터를 구하는 곳 셋, 눈에 띄는 카드 하나** (`61356a8` · `d4e38c9` · `26c009d`)

12절의 2열 카드 그리드(`a18752a`)에 카드 셋을 더했다. 아홉일 때 **캐글이 왼쪽에 혼자** 서던 자리가 채워졌다.

| 카드 | 성격 | 출처 줄 |
|---|---|---|
| **네이버 데이터랩(DataLab)** | 검색어 · 쇼핑 클릭 추이를 기간 · 기기 · 성별 · 연령으로 비교 — 관광 키워드의 관심이 **언제** 오르내리나 | 네이버 · 검색어 트렌드 |
| **KOSIS 통계시각화콘텐츠** | 인구 · 물가 등 주요 지표를 차트 · 지도 · 웹툰으로 — **보는** 곳 | 통계청 KOSIS · 통계 시각화 보기 |
| **KOSIS 데이터 시각화 체험하기** | 데이터를 골라 차트를 직접 — **만드는** 곳 | 통계청 KOSIS · 시각화 직접 만들기 |

**KOSIS 둘은 같은 기관이라 「보기 / 직접 만들기」로 갈라 적었다.** 따로 만들어 달라는 요청이었고,
이름만 보면 둘이 같은 서비스로 읽힌다. 두 주소 모두 **열어서 페이지 제목을 확인한 뒤** 설명을 썼다.

> ~~12곳이 되어 2열 격자가 짝이 맞는다~~ — **틀렸다.** 같은 시간 16절의 `3f21dc1` 이 한 칸을 더했고,
> 17절에서 KOSIS 둘을 합쳐 지금은 **열하나**다(아래 17절).

### 공공 데이터 카드만 진한 초록으로 (`26c009d`)

「02 · 관광 데이터는 어디서 오는가」 여섯 카드 중 **공공 데이터**만 `#2f5148` 바탕 · 흰 글자로 세웠다.
클래스를 바꾸지 않고 `t-green` 에 **`solid` 를 더했다** — 9/12 교훈(「클래스를 떼지 말고 변형자를 더한다」) 그대로다.

```css
.vcards > li.solid        { background:#2f5148; border-color:#2f5148; border-top-color:#1f382f; }
.vcards > li.solid .vt    { color:#cfe6dc; }   /* 꼬리표 「공식」만 옅게 — 제목과 갈린다 */
.vcards > li.solid .vh,
.vcards > li.solid .vd    { color:#fff; }
```

「03 · 분석은 다섯 걸음으로」의 초록 카드(시각화 · 해석)는 요청 밖이라 그대로 뒀는데, 곧이어 **다른 세션이 `ac04ea2` 로 같은 모양을 입혔다** — 이제 두 섹션의 초록 카드가 둘 다 진한 초록이다.

---

## 15. **공모 상자 둘 — 데이터랩(9/30)을 부산(9/18) 위에** (`a9b46c2`)

사용자가 데이터랩 공지 URL 하나를 주며 **「이거 위에 올려주고, 연한 베이지로 2개를 통일해줘」**라 했다.
열어 보니 **2026 한국관광 데이터랩 활용 경진대회**(한국관광공사 · 접수 8/3 ~ **9/30** ·
총상금 **2,000만원** · **문화체육관광부 장관상 · 한국관광공사 사장상**)였다.

### 왜 부산 위인가

| | 부산 Big Data 활용 대회 | 데이터랩 활용 경진대회 |
|---|---|---|
| 마감 | **9/18(금) 17:00** | **9/30** |
| 내는 것 | 보고서 · 시각화 · **데이터 파일** | **데이터를 써서 무엇이 달라졌는가** |
| 성격 | 분석 결과물 | 문제 해결 · 성과 사례 |

**마감은 부산이 빠른데 자리는 데이터랩을 위에 놓았다.** 이 모듈이 02장부터 내내 쓰는
데이터가 데이터랩이고, 통신 · 카드 · 내비게이션이 전부 거기 모여 있기 때문이다.
설명 줄에는 **「두 대회를 한 벌로 준비하라」**를 넣었다 — 열이틀 차이라
**부산에 낸 결과물을 각만 바꿔 이어서 낼 수 있다.**

> **공고 본문에 없는 것을 적어 두었다** — 부문 구분 · 등수별 상금 · 심사 일정이
> 본문에는 없고 **첨부 요강 PDF** 에만 있다. 「먼저 할 것」 칸에 **반드시 내려받아
> 확인하라**고 못박았다. 없는 것을 그럴듯하게 채우면 학생이 잘못 준비한다.

### 연한 베이지로 통일

```
전  샛노랑 #ffe800 그러데이션 + 검은 글자        ← 하나일 때는 잘 걸렸다
후  연한 베이지 #faf3e3 → #f0e5cb + 고동 글자   ← 둘이 나란히 서니 눈이 부셨다
```

둘 다 **「지금 낼 수 있는 것」**이라 같은 톤이어야 한 벌로 읽힌다.
**갈라 둔 것은 마감 알약 하나다** — 부산(9/18)은 붉게 채우고, 데이터랩(9/30)은 흰 바탕에
테두리만. 급한 것과 여유 있는 것이 색으로 구분된다.

---

## 16. **관광빅데이터분석 — 같은 페이지를 두 세션이 나눠 만졌다** (8절 세션, 13:10 ~)

14 · 15절과 **같은 시간, 같은 파일**이다. 마지막 절 번호를 읽고 16 로 붙였다.

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `182cc4f` | 이 일지 8절 번호 정리 | 6 · 7 이 다른 세션 절과 겹쳐 8 로 |
| `cc950e0` | 학사일정 설명 **네 문장 → 한 줄** 「관광빅데이터분석은 월요일 1·2·3교시입니다.」 · 작은 글씨 줄의 **「세 과목 공통」 삭제** | 관광벤처창업론 같은 자리(「화요일 5·6·7교시입니다」)와 길이를 맞춤. 16주 · 보강일은 작은 글씨 줄에 남음 |
| `7709eab` | **데이터 구하기 넷**(서울 열린데이터광장 · MDIS · Awesome Public Datasets · 구글 데이터셋 검색) → 2열 카드 | 같은 목록 끝 **경진대회 3곳** 칸은 알약 + 설명 세 줄 묶음이라 카드에서 빼 아래 원래 모양으로 |
| `3f21dc1` | **분야별 빅데이터 플랫폼 5곳**을 첫 카드 그리드 **맨 아래 한 칸**으로 | 앞 소개 문단을 지우고 **Boardmix 출처는 카드 meta 줄**로 옮김. `lcard .grouptitle` 15px |
| `ac04ea2` | 분석 다섯 걸음의 **「시각화·해석」 → 진한 초록 바탕 흰 글자** | 14절이 만든 `li.solid` 를 붙였다 — 클래스 한 단어 |

⚠️ **첫 카드 그리드의 칸 수가 홀수다** — 커밋 뒤 세었을 때 열셋, 다른 세션이 KOSIS 둘을 국가통계포털 카드에 합친 `6bc1517` 뒤에는 **열하나**. 14절이 셋을 더해 「열둘, 짝이 맞는다」고 적은 시점과
이 절의 `3f21dc1`(플랫폼 카드 하나)이 겹쳐, **마지막 카드가 다시 혼자 선다.** 하나를 빼거나 하나를 더할지 정해야 한다.

> **교훈 — 같은 파일을 두 세션이 나눠 만지면 「짝이 맞았다」 같은 셈이 서로의 커밋에서 틀어진다.**
> 격자에 칸을 더하는 커밋은 **넣은 뒤의 전체 칸 수**를 적는다.

> **교훈 — 「이것을 여기 하단에 하나의 칸으로」는 옮기는 요청이다.** 블록만 옮기면 앞의 소개 문단이
> **빈 자리를 소개**하게 된다. 지울 때는 그 안의 **출처 · 인용**을 살릴 자리부터 찾는다.

---

## 17. **KOSIS 셋을 한 카드로** (`6bc1517`)

14절에서 따로 만든 KOSIS 카드 둘을 사용자가 **「국가통계포털(KOSIS) 카드와 함께 제시해줘」**라 했다.
옆에 두는 방법도 있었지만 2열 격자에서는 **줄이 갈려** 함께 읽히지 않는다(KOSIS 가 오른쪽 칸이었다).
그래서 **한 카드 안으로** 넣었다 — 같은 목록의 「분야별 빅데이터 플랫폼 · 5곳」 카드와 같은 문법이다.

```
국가통계포털(KOSIS) ↗
  [통계시각화콘텐츠 ↗] [데이터 시각화 체험하기 ↗]     ← linkrow tiny
  포털 설명
  통계시각화콘텐츠 — … 보는 곳.
  데이터 시각화 체험하기 — … 만드는 곳.
  통계청 · 국가통계 · 시각화 보기 · 직접 만들기       ← meta
```

**설명은 14절 문장을 그대로 옮겼고 새로 쓴 것은 「보는 곳 / 만드는 곳」 꼬리 둘뿐이다.**
따로 있던 카드 둘은 지워 **같은 링크가 두 번 나오지 않는다.**

**칸 수 — 커밋 뒤 직접 셌다. 첫 그리드는 열하나(`<li>` 11).** 16절 경고대로 **마지막 카드가 혼자 선다.**
합치기 전 열셋도 홀수였으니 이번 합침이 만든 문제는 아니지만, 짝을 맞출지는 아직 정하지 않았다.

> **파일이 CRLF 였다** — 12절 교훈대로 줄바꿈을 감지해 **원래 형식으로 되돌려 썼다.**
> 커밋 뒤 `git pull --rebase` 가 「여러 브랜치로 리베이스할 수 없다」로 멈춰 `fetch` → `rebase origin/master` 로 올렸다.

---
## 18. **키워드 도구 셋을 한 묶음으로** (`7ca560e`)

사용자가 URL 둘(`itemscout.io` · `pandarank.net`)을 주며
**「썸트렌드 · 블랙키위와 함께, 네이버 데이터랩에 이어 그리드 하단에」**라 했다.

**두 사이트를 직접 열어 확인했다** — 소개 문구를 지어내지 않기 위해서다.

| 도구 | 운영사 | 무엇을 재나 |
|---|---|---|
| 네이버 데이터랩 | 네이버 | 관심이 **언제** 오르내리는가 |
| **아이템스카우트** | ㈜문리버 | 그 관심이 **실제 거래로 이어지는가** — 한 달 검색수 · 경쟁강도 · 실거래 비율 · 평균가격 |
| **판다랭크** | ㈜선데이띵커 | 그 키워드로 **글을 썼을 때 사람이 오는가** — 검색량 대 일 방문자, 채널 영향력·신뢰도 지수 |

**설명에 셋의 역할 차이를 세웠다.** 그냥 나열하면 「키워드 도구가 셋 있다」로 끝나지만,
**같은 키워드를 세 각도로 재는 흐름**으로 적으면 학생이 무엇을 먼저 열지 안다.
판다랭크의 인플루언서 검색은 **06장 홍보**와 바로 이어진다고 붙여 두었다.

> **막힌 곳** — `git pull --rebase origin master` 가 **`fatal: Cannot rebase onto multiple branches`** 로
> 죽었다. `stash` 는 이미 걸린 뒤였다. `fetch` 로 보니 앞서지도 뒤지지도 않은 상태(0/0)였고,
> `stash pop` 으로 되살려 그냥 커밋했다. **작업은 스태시에 온전히 남아 있었다.**

---

## 19. **분야별 플랫폼 카드를 KOSIS 아래로** (`30d0d9c`)

사용자가 KOSIS 카드 내용을 옮겨 적고 **「이것 옆에 [분야별 빅데이터 플랫폼 5곳] 이것을 제시해줘」**라 했다.

분야별 카드는 그 사이 다른 세션이 **전체 폭(`li.wide`)** 으로 바꿔 두었고,
그리드 **맨 아래**에 혼자 떨어져 있었다. KOSIS 다음 줄로 올렸다.

```
3 공공데이터포털   │ 4 KOSIS
5 분야별 빅데이터 플랫폼 · 5곳  ← 전체 폭으로 한 줄을 통으로
6 빅카인즈        │ 7 텍스톰
```

**국가 공식 통계 → 부처·기관별 분야 특화**로 한 흐름이 됐다.
전체 폭 카드라 「옆에」 나란히는 설 수 없어, **바로 아래 줄을 통으로 받게** 두었다.

> **막힌 곳 셋.** 이 한 건에서 스크립트가 네 번 죽었다.
> ① `<li>` 만 세다 **`<li class="wide">` 를 놓쳐** 13을 12로 셌다.
> ② 항목 사이 공백을 빈 문자열로 가정해 「부스러기 없음」 검사가 실패했다.
> ③ 진행 출력에 **em dash** 를 넣어 `cp949` 콘솔에서 `UnicodeEncodeError` 로 죽었다 —
> 이번 주 세 번째다. **파일 쓰기 전에 출력이 죽으면 아무것도 안 남는다.**
> ④ `git pull --rebase` 가 `Cannot rebase onto multiple branches` 로 죽었다(18절과 같은 증상).

---

## 20. **ADsP · ADP 카드를 진한 하늘색으로** (`d44a7e9`)

자격증 카드 줄에서 **「단계별 검증 · ADsP · ADP」** 하나만 진한 하늘색 바탕 · 흰 글자로 세웠다.

14절에서 만든 `li.solid`(진한 초록 · 흰 글자)를 그대로 쓰고, **바탕색만 바꾸는 변형자 `sky`** 를 더했다.
클래스는 `t-blue solid sky` — 원래 `t-blue` 를 떼지 않았다.

```css
.vcards > li.solid.sky     { background:#1f7fb8; border-color:#1f7fb8; border-top-color:#155f8c; }
.vcards > li.solid.sky .vt { color:#d4ecf8; }   /* 꼬리표만 옅게 — .vh · .vd 흰 글자는 solid 에서 물려받는다 */
```

**4줄 추가 · 1줄 수정**뿐이다(`git show --stat` 로 확인). 파일은 CRLF 라 원래 형식으로 되돌려 썼다.

> 이제 이 페이지에 **꽉 찬 카드가 셋**이다 — 공공 데이터 · 시각화 · 해석(진한 초록), ADsP · ADP(진한 하늘).
> 9/12 교훈 「강조는 늘릴수록 약해진다」가 닿는 수다. 하나 더 늘리기 전에 줄일 곳을 먼저 본다.

---

## 21. **홀수 칸을 두 칸 폭 하나로 풀었다** (`ca625c0`, 16절 세션)

16절에서 남긴 「첫 그리드가 홀수라 마지막 카드가 혼자 선다」를 **칸을 빼거나 더하지 않고** 풀었다.

사용자 요청 둘이 겹쳤다 — **「판다랭크를 아이템스카우트 옆에」**, 이어서 **「썸트렌드 · 블랙키위를 가장 아래에」**.
18절(`7ca560e`)이 판다랭크를 넣었지만 열셋째 칸이라 **아이템스카우트 다음 줄에 혼자** 서 있었다.

| 한 것 | 왜 |
|---|---|
| 분야별 플랫폼 칸에 `li.wide`(`grid-column: 1 / -1`) | 알약 다섯 + 설명 다섯 줄이라 원래 가장 긴 칸이다. **두 칸 폭으로 한 줄을 통으로** 쓰게 하면 나머지 열둘이 짝이 맞는다 |
| 썸트렌드 · 블랙키위를 맨 아래로 | 요청. 둘이 한 줄이 된다 |
| 결과 | 아이템스카우트 │ 판다랭크가 한 줄 |

뒤이어 19절(`30d0d9c`)이 그 넓은 칸을 **KOSIS 바로 아래로** 올렸다. 넓은 칸은 한 줄을 통으로 쓰므로
**어디로 옮겨도 짝은 깨지지 않는다** — 커밋 뒤 다시 셌다.

```
1 K-MOOC          │ 2 서울 빅데이터 캠퍼스
3 공공데이터포털   │ 4 KOSIS
5 분야별 빅데이터 플랫폼 · 5곳 (두 칸 폭)
6 빅카인즈        │ 7 텍스톰
8 캐글            │ 9 네이버 데이터랩
10 아이템스카우트  │ 11 판다랭크
12 썸트렌드       │ 13 블랙키위
```

> **교훈 — 홀수 격자는 칸을 빼거나 더하지 않아도 풀린다.** 가장 긴 칸 하나를 **전체 폭**으로 세우면
> 짝 맞추기가 그 칸의 위치와 무관해진다. 이후 누가 칸을 옮겨도 깨지지 않는 쪽을 고른다.

---

## 21-2. **공공데이터포털 ↔ 분야별 플랫폼 맞바꿈 · 서울시 대회 수상작 영상** (`48dbe0f` · `8c74a4b`)

### 카드 자리 맞바꿈 (`48dbe0f`)

사용자가 두 카드 내용을 옮겨 적고 **「이거 위치 체인지」**라 했다. 두 `<li>` 블록을 **통째로 맞바꿨고 글은 한 글자도 안 바꿨다**(16줄 삭제 · 16줄 추가).
21절 그림이 곧바로 옛것이 됐다 — 지금은 이렇다.

```
1 K-MOOC          │ 2 서울 빅데이터 캠퍼스
3 분야별 빅데이터 플랫폼 · 5곳 (두 칸 폭)
4 KOSIS           │ 5 공공데이터포털
6 빅카인즈        │ 7 텍스톰   … 이하 21절과 같다
```

21절 교훈대로 넓은 칸이라 **옮겨도 짝은 깨지지 않았다.** 다만 19절이 만든
**「국가 공식 통계 → 분야 특화」 흐름은 거꾸로**(분야 특화 → 국가 통계 · 공공데이터) 됐다. 사용자에게 확인을 청해 두었다.

> 블록을 찾을 때 `\n        <li[^>]*>.*?\n        </li>` 로 **들여쓰기까지 붙여** 잡았다 —
> 분야별 칸 안의 `linkrow` 가 중첩돼 있어도 **바깥 `</li>` 만** 끝으로 걸린다(9/12 17절의 「첫 닫는 태그」 사고를 피하는 법).

### 서울시 빅데이터 활용 경진대회에 수상작 영상 (`8c74a4b`)

「2026 빅데이터 · AI 공모전 안내」(`bigdata-ai-competitions-2026.html`) **⑤ 칸**, 목록과 공고 알약 줄 **사이**에 한 줄.

| | |
|---|---|
| 알약 | 「▶ 2026 분석 부문 최우수상 발표 영상 · 7:10부터 ↗」 — 흰 바탕 · 붉은 테두리 · 11.5px(공고 알약보다 작게) |
| 링크 | `youtu.be/3vGNw3ApFk8?t=430` — **공유 추적 꼬리 `si=…` 는 뺐다** |
| 곁글 | 서울특별시 빅데이터캠퍼스 유튜브 — 수상팀이 무엇을 문제로 잡고 어떻게 보여 줬는지 |

**영상 제목은 oEmbed 로 확인했다** — 「2026 서울시 빅데이터 활용 경진대회(분석 부문) "최우수상"」, 채널 서울특별시 빅데이터캠퍼스.
사용자가 준 것은 주소뿐이라, 알약 이름의 「분석 부문 최우수상」은 **확인한 제목에서** 가져왔다.

「서울시 빅데이터」는 관광빅데이터분석의 **빅데이터 캠퍼스 카드**에도 있으나, 「공모전」이라 해서 경진대회 칸을 골랐다.

> 번호 — 다른 세션이 같은 시간에 22 · 23 · 24 를 붙여 **「22」가 겹쳤다.** 남의 번호는 두고 내 절을 21-2 로 매겼다.
> 이 영상 줄은 **23절(공고 알약)과 24절(수상작 칸) 사이에 커밋**됐고, 24절이 칸을 2열로 바꾼 뒤에도 **살아 있음을 grep 으로 확인**했다.

---
## 22. **KOSIS 안의 알약 둘을 샛초록으로** (`2e3092c`)

「통계시각화콘텐츠 · 데이터 시각화 체험하기」 둘이 **흰 바탕에 회색 테두리**라
카드 배경(`#f4f8f6` 그러데이션) 위에서 묻혀 있었다. 링크가 아니라 장식처럼 보였다.

```css
.reslist .linkrow.fill-green a { background: #1faa59; border-color: #1faa59; color: #fff; font-weight: 800; }
```

**왜 채웠나** — 이 둘은 바깥 링크가 아니라 **같은 기관(통계청)의 다른 창구**다.
목록의 다른 카드처럼 흰 알약으로 두면 「또 다른 사이트」로 읽힌다.
채워서 **KOSIS 안에서 갈라지는 두 길**(보는 곳 · 만드는 곳)로 보이게 했다.
`.fill-green` 은 같은 성격의 묶음이 또 생길 때 그대로 붙여 쓴다.

---

## 23. **서울시 빅데이터 경진대회 — 칸은 있었는데 링크가 없었다** (`2b91297`)

사용자가 URL 하나(`news.seoul.go.kr/gov/archives/576001`)를 주며
**「노란색으로 빅데이터 공모전에 넣어라」**라 했다. 뒤이어 **「연한 노랑으로」**.

**열어 보니 마감이 이미 지나 있었다** — 접수 3/16 ~ **5/13**. 오늘이 9/14 다.
그래서 「지금 낼 수 있는 것」이 아니라 **「2027년을 대비해 볼 대회」 ⑤번 칸**이 자리였고,
그 칸은 이미 있는데 **링크가 없었다.** 이 페이지에서 **처음으로 바깥 공고로 나가는 링크**다.

**두 줄뿐이던 칸을 공고에서 확인한 것으로 채웠다.**

| | |
|---|---|
| 2026 일정 | 접수 3.16 09:00 ~ **5.13 18:00** · 1차 5.14~21 · 2차 5.26~6.2 · **최종경연 7.6** 서울시청 8층 |
| 부문 셋 | 창업 · 분석 · 시각화 |
| 자격 | 제한 없음, 개인 또는 **최대 5인 팀** |
| 시상 | 창업 8팀 1,150만 · 분석 7팀 600만 · 시각화 7팀 600만 = **22팀 2,350만원** |
| 문의 | 02-2133-1271 |

알약은 **연한 노랑**(`#fdf3c8`). 지난 대회라 급하지 않고 **미리 읽어 두는 자리**여서,
「지금 낼 수 있는 것」의 붉은 마감 알약과 톤을 갈랐다.
옆 설명에는 **부문이 셋이라 두 과목이 갈라져 들어간다**고 적었다 —
분석 · 시각화는 이 수업 산출물, **창업 부문은 관광벤처창업론 사업계획서**.

---

## 24. **그 옆에 수상작 칸 — 요강과 성격이 다르다** (`cf4f502`)

사용자가 `data.seoul.go.kr/bsp/wgs/contest26.do` 를 주며 **「서울시 빅데이터 옆에 새로운 그리드로」**라 했다.
열어 보니 **시각화 부문 수상작 전시 화면**이었다.

```
┌────────────────────┬────────────────────┐
│ ⑤ 서울시 경진대회   │ ⑤-1 서울 데이터 허브│
│    요강             │    수상작 · 양식    │
│ 무엇을 내라         │ 어느 수준으로 내라  │
└────────────────────┴────────────────────┘
```

| 오른쪽 칸에 담은 것 | |
|---|---|
| 수상작 | 2026년 **일곱**(대상·최우수·우수·특별 4) + 2025년 **여섯** |
| 가장 쓸모 있는 것 | 수상작마다 **발표자료 PDF** · **대화형 시각화 체험 링크** |
| 양식 | 참가신청서 · 참여 각서 · 개인정보 동의서(HWP·DOCX) · **발표 PPT 템플릿** |

오른쪽은 아주 연한 노란 바탕(`#fffdf4`)으로 갈랐고, 820px 아래에서는 쌓인다.

> **알약 옆에 붙인 한 줄** — **「공고보다 이쪽을 먼저 여십시오.
> 요강은 무엇을 내라는 말이지만, 수상작은 어느 수준으로 내야 하는가를 보여 줍니다.」**
> 학생이 공모전에서 가장 못 가늠하는 것이 **완성도의 기준**이다.
> 수상작 열셋을 직접 열어 보는 것이 요강 열 번 읽는 것보다 빠르다.

---

## 25. **수상작 영상을 수상작 칸으로** (`62894c6`)

21-2절에서 요강 칸(⑤)에 넣은 **분석 부문 최우수상 영상** 줄을, 24절이 새로 세운 **⑤-1 수상작 칸**으로 옮겼다.
사용자에게 「한데 모을까요」를 묻고 **「진행해라」**를 받았다.

| | 전 | 후 |
|---|---|---|
| 자리 | ⑤ 요강 칸 · 목록 아래 | **⑤-1 수상작 칸** · 목록 아래, 「수상작 보기」 알약 위 |
| 알약 | ▶ **2026** 분석 부문 최우수상 발표 영상 · 7:10부터 | ▶ 분석 부문 최우수상 발표 영상 · 7:10부터 |
| 곁글 | 서울특별시 빅데이터캠퍼스 유튜브 — … | **시각화 말고 분석 부문은 발표 영상으로 봅니다** — … |

**곁글 첫머리를 바꾼 이유** — ⑤-1 칸 제목이 「**시각화 부문**」이다. 그대로 옮기면
시각화 칸 안에 **분석 부문 영상**이 까닭 없이 서 있게 된다. 둘이 다른 부문이라는 것을 첫 말에서 밝혔다.
`.vid-s strong { color:#123b67 }` 한 줄을 더해 「분석 부문」이 옆 `cta-n` 강조색과 같게 했다.

> **이제 ⑤ 줄이 성격대로 갈렸다** — 왼쪽은 **무엇을 내라**(요강 · 공고), 오른쪽은 **어느 수준으로**(시각화 수상작 · 분석 수상 영상).

### 막힌 곳 — 줄바꿈과 동시 편집

- `sed` 로 CSS 한 줄을 넣었더니 **CRLF 파일에 LF 한 줄**이 섞였다. 커밋 뒤 파일 전체를 CRLF 로 되돌렸다(내용 변화 없음).
  **12절 교훈(「줄바꿈을 감지해 원래 형식으로」)을 스크립트에선 지키고 `sed` 에선 놓쳤다.**
- 커밋 직후 `rebase` 가 「unstaged changes」로 멈췄다 — 다른 세션이 같은 순간 작업 중이었다.
  **커밋은 이미 되어 있었고**, 그 세션의 `83e5e4a` 가 위에 얹히며 함께 올라갔다. `git branch -r --contains 62894c6` 로 원격에 있음을 확인했다.

---

## 25. **서울시 대회를 본문에도 세웠다 — 공모 상자가 셋이 됐다** (`8179419`)

23절에서 서울시 대회를 **공모전 페이지에만** 넣고 **본문에는 넣지 않았다.**
사용자가 **「부산 아래에 넣어 달라고 했는데 여전히 안 보여」**라 해서 알았다.
부산 상자 바로 아래에 세웠다.

**색이 급한 순서를 말하게 했다.**

| | 상자 | 바탕 | 배지 | 마감 알약 |
|---|---|---|---|---|
| 1 | 데이터랩 (9/30) | 베이지 `#faf3e3` | 지금 낼 수 있는 것 | 흰 바탕 |
| 2 | 부산 (9/18) | 베이지 `#faf3e3` | 지금 낼 수 있는 것 | **붉게 채움** |
| 3 | 서울시 (끝남) | **더 옅은 `#fdfaf2`** | **2027년 대비** | 흰 바탕 |

첫 문장이 이 상자의 이유다 — **「지금 낼 수는 없지만 지금 읽어야 하는 대회입니다.
접수가 3월에 열려 5월에 닫힙니다 — 그때 알면 늦습니다.」**

> **되풀이된 것** — 사용자가 「안 보인다」고 두 번 말했다. 한 번은 **내가 정말 안 넣었고**,
> 한 번은 **넣었는데 브라우저 캐시**였다. 두 번째는 두 호스트를 `curl` 로 받아
> 문자열 위치까지 확인해 보여 주었다. **「배포했습니다」로 끝내지 말고,
> 안 보인다고 하면 라이브를 직접 받아 확인한다.**

---

## 26. **KOSIS · 캠퍼스 · 데이터랩 — 카드 안에 창구를 넣었다** (`4af2d35` ~ `b2185ee`)

같은 규칙이 세 카드에 반복됐다 — **한 기관의 카드 안에, 그 기관의 다른 창구를 샛초록 알약으로.**

| 카드 | 안에 넣은 알약 |
|---|---|
| 국가통계포털(KOSIS) | 통계시각화콘텐츠 · 데이터 시각화 체험하기 |
| 서울특별시 빅데이터 캠퍼스 | 경진대회 수상작 · 양식 |
| **한국관광 데이터랩**(신설) | 지역별 관광 현황 · 활용 우수사례집 PDF · 수상팀 인터뷰 5편 · 활용 경진대회 |

**데이터랩 카드는 새로 만들었다.** 이 모듈이 02장부터 내내 쓰는 데이터인데
정작 관련 링크에는 없었고 공모 상자 안 알약으로만 있었다.
설명에 공공데이터포털과의 차이를 세웠다 — **전 분야를 모은 창고 ↔ 관광만 골라 둔 곳.**

**공공데이터포털과 캠퍼스의 자리를 맞바꿨다.** 「데이터 수집의 출발점」이라 써 놓고
다섯째에 있었고, 앞자리는 **회원가입 · 반출 승인이 필요한** 캠퍼스가 차지하고 있었다.

> **덤** — 카드가 13 → 14 가 되어 일지 「남은 것」의 **2열 격자 홀수 문제**가 함께 풀렸다.

---

## 27. **영상은 결과, 노트는 과정** (`7456b26`)

서울시 상자 안에 **분석 부문 최우수상 발표 영상**을 끼우고 그 아래 **연구노트**를 걸었다.

```html
<iframe src="https://www.youtube-nocookie.com/embed/3vGNw3ApFk8?start=430" loading="lazy" …>
```

**화면을 다 채우지 않는다** — `max-width: 560px` · `aspect-ratio: 16/9`.
사용자가 명시한 조건이고, 발표 영상은 본문 폭을 다 쓸 이유가 없다.
`youtube-nocookie` 와 `loading="lazy"` 로 페이지가 무거워지지 않게 했다.

**연구노트는 (2)를 썼다.** (1)과 `diff` 해 보니 (2)에 **공유 링크와 여백 조정**이
더해져 있었다 — 사용자 말대로 최신이다.

> **설명에 세운 한 줄** — **「영상은 결과를 보여 주지만, 연구노트는 어떻게 거기까지
> 갔는지를 적었습니다.」** 일곱 걸음(관광지 44곳 선정 → 로드뷰 수집 → 멀티모달
> AI(MLLM) 평가 → 신뢰도 검증 → 키워드 · 토픽 분석 → 정책 우선순위 → 지도 시각화)과,
> 「관광객은 주민과 달리 낯선 공간을 탐색하며 이동한다」에서 출발해
> **「지하철역에서 관광지까지의 길도 관광 경험」**으로 이어지는 문제 정의를 짚었다.
> **03장 다섯 걸음이 실제로 어떻게 돌아가는지** 보는 자리다.

같은 규칙으로 데이터랩 카드에도 **사례집(결과) ↔ 인터뷰(과정)** 의 차이를 적었다.

---

## 28. **오후 — 작은 글씨 하나, 색 하나, 제목 하나, 실습 예제 둘** (16절 세션)

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `96f2ae0` | 강의실 바 「사회대 2106호 1층 PC실」 옆에 **「SPSS 사용 가능」** 작은 회색 글씨(`.rm .sw`) | 관광빅데이터분석만. 스마트투어리즘 · 관광벤처의 같은 2106호에는 넣지 않았다 — 넣을지 물어 둠 |
| `4e54bd7` | **군집 분석(Clustering)** 카드 → 진한 보라(`#4b3a7a`) · 흰 글자 | 20절 `solid.sky` 와 같은 방식으로 `solid.violet` 변형 하나 더. 꼬리표 「비지도학습」만 연보라 |
| `06656d7` | 07절 제목 「부산, 그리고 이 사이트에서」 → **「빅데이터 공모전」** | ⚠️ 제목 바로 아래 첫 문단은 **부산 혼잡 · 축제 유동인구 사례**라 제목과 어긋난다. 옮길지 물어 둠 |
| `83e5e4a` | KOSIS 카드에 **실습 예제 · 경북 외국인등록인구 해석**(초록 알약, 시각화 둘 옆) + 아래 줄 **KOSIS 시각화 결과 해석 프롬프트 · 학생용** | 파일은 `kosis-gyeongbuk-foreign-residents-dikw.html` · `kosis-visualization-interpretation-prompts.html`. 카드 설명에 두 줄 추가 |

### 올리기 전에 말투 13곳 (`83e5e4a`)

9/13 규칙대로 grep 했다. 둘 다 **교수자가 AI에게 받은 답을 그대로 저장한 판**이었다.

| 파일 | 전 | 후 |
|---|---|---|
| 경북 DIKW | 사용자가 제시한 그래프는 | 이 그래프는 |
| 경북 DIKW | 학생들에게는 이렇게 설명하면 쉽습니다 | 이렇게 기억하면 쉽습니다 |
| 프롬프트 | 그래프를 보고 이렇게 **말하게 하기** | 이렇게 **말하기** |
| 프롬프트 | **학생이** 반드시 물어볼 것 · **학생의** 목표는 · **수업에서 강조할** 핵심 | 반드시 물어볼 것 · 목표는 · **기억할** 핵심 |
| 프롬프트 | …코치로 활용하도록 **유도할 수 있습니다** | …코치로 활용**하게 됩니다** |

`「학생들이 <span>…」` 처럼 **낱말 사이에 태그가 낀 곳**은 첫 치환에서 0건으로 빠져 한 번 더 잡았다.
**「학생용」이라는 제목 · 알약 이름은 남겼다** — 누구를 위한 자료인지 알리는 말이지 교수자 시점이 아니다.

> ⚠️ **프롬프트 파일 끝에 ChatGPT 공유 링크가 그대로 있다.** 사용자가 「공유링크포함」을 최신이라 골랐으니 뒀지만,
> 공개 페이지라 **그 대화 전체를 누구나 연다.**

### 남의 반쯤 된 작업이 내 커밋에 딸려 갔다 — **깨진 링크가 몇 초 배포됐다**

`git add learn/big-data-analytics.html` 을 **파일째** 했다. 그 순간 다른 세션이 같은 파일에
**서울시 최우수상 영상 + 「이 발표를 뜯어 본 연구노트 ↗」 알약**을 넣어 둔 상태였고, 알약이 가리키는
`seoul-bigdata-2026-award-notes.html` 은 **아직 커밋 전(untracked)** 이었다.

```
83e5e4a (내 커밋)   big-data-analytics.html 27줄  ← 내 것은 8줄, 19줄은 남의 것
                   seoul-bigdata-2026-award-notes.html 없음 → 알약이 404
7456b26 (다른 세션) 연구노트 파일 커밋 · 푸시 → 정상화
```

커밋 **뒤에** `git show HEAD --stat` 을 보고 알았다(예상 8줄 ↔ 실제 27줄). 다행히 수십 초 뒤 다른 세션이 파일을 올렸다.

> **교훈 — 커밋 전 `git status` 에 내 것이 아닌 `??` 가 보이면, 같은 파일에 남의 미커밋 수정이 있다는 신호다.**
> 이번엔 `?? seoul-bigdata-2026-award-notes.html` 이 떠 있었는데 지나쳤다. 그때는 **9/13 12절 방법(HEAD 본 + 내 치환만 → `update-index`)** 으로 갔어야 한다.
> 「파일째 add」는 **`git diff --stat` 의 줄 수가 내 예상과 같을 때만** 한다.

---
## 29. **영상을 가운데로, 도구 카드마다 「그 도구로 쓴 논문」** (`9727abf` ~ `958fc46`)

### 영상 가운데 정렬 (`9727abf`)

폭이 **560px** 이라 상자 왼쪽에 붙으니 오른쪽이 비어 한쪽으로 쏠렸다.
`margin: 0 auto` 로 가운데에 세우고 설명도 가운데 정렬로 맞춰 **영상과 한 덩이**로 읽히게 했다.
아래 연구노트 알약 줄은 **왼쪽 정렬 그대로** 두었다 — 상자 안 다른 알약 줄과
줄맞춤이 어긋나면 왼쪽 기준선이 흐트러진다.

### 분석 도구 카드에 실제 논문을 붙였다 (`ad74bea` ~ `958fc46`)

**텍스톰 셋 · 빅카인즈 하나.** 도구 소개만 있으면 학생은 「그래서 이걸로 뭘 하나」에서 멈춘다.
**그 도구를 실제로 쓴 논문**을 옆에 두면 산출물의 모양이 보인다.

| 카드 | 붙인 알약 |
|---|---|
| 텍스톰(TEXTOM) | 텍스톰으로 쓴 논문 · 원문 PDF · **빅카인즈 + 텍스톰** 여가 키워드 논문(ScienceON) · 한국관광 트렌드 감성분석 논문(DBpia) |
| 빅카인즈(BIG KINDS) | 빅카인즈로 쓴 논문 · 플로깅 트렌드 분석(ScienceON) |

**「빅카인즈 + 텍스톰」 논문이 두 카드를 잇는다** — 뉴스에서 기사를 긁고(빅카인즈)
그 텍스트를 분석하는(텍스톰) 순서가 한 편 안에 들어 있어, 두 도구가 왜 나란히 서 있는지가 논문으로 설명된다.

### 서울 빅데이터 캠퍼스 카드에도 알약 셋 (`ddc1d62`)

수상작 알약 하나였던 것이 **공고 원문 · 수상 영상(7:10부터) · 공모전 여섯**까지 넷이 됐다.
26절의 규칙(**한 기관 카드 안에 그 기관의 다른 창구**)이 여기서도 이어진다.

> **오늘 반복된 짜임 하나** — 카드마다 **「이 도구/기관이 무엇인가」 다음에 「그래서 무엇이
> 나오는가」**를 알약으로 붙였다. KOSIS 는 시각화 체험, 데이터랩은 사례집 · 인터뷰,
> 캠퍼스는 수상작 · 영상, 텍스톰 · 빅카인즈는 논문.
> **도구를 소개하는 페이지가 아니라 산출물을 보여 주는 페이지**가 되어 간다.

---

## 30. **노란 알약 셋째, 지난 공고 하나, 빨간 최종결과물 · 초록 인바운드** (16절 세션)

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `1d773c0` · `21a2d6e` | 관광빅데이터분석 한국관광 데이터랩 카드에 **노란 알약 「대상 사례 PM 안전 · DIKW와 프롬프트」**(`datalab-pm-safety-dikw-prompts.html`) | 「수상팀 인터뷰 5편」 옆 — 그 인터뷰의 **대상 사례(전동 킥보드 사고 사각지대)** 를 뜯어 본 판이라 붙였다. 초록 줄 안에서 `a.ylw` 로 갈라 세움. 카드 설명에 한 줄 |
| `154833d` | 관광벤처 창업 지원 ② K-Startup 칸에 **「창업중심대학 예비창업자 모집 공고 (2024)」** | 링크를 열어 보니 **2024.01.12 공고(제2024-31호)** — 이미 지난 것이라 이름에 **(2024)** 를 붙였다 |
| `73e2eff` | 관광벤처 강의실 바 **「최종결과물 · 사업계획서 한 부」 노랑 → 빨강**(`#c0392b`) | 사용자가 **「여기서 최종 결과물만 !!!」** 「현재 노란색 알약 이거든」 — 파트 알약 여덟은 그대로. `a.fin` 이 페이지에 하나뿐임을 셌다 |
| `fed3bdf` | 07장 **「인바운드 여행시장 — 산업구조와 경쟁전략 18섹션」 빨강 → 07장 기본 초록**(`hot`, `#1f6a73`) | 4절에서 「받는 쪽은 다르다」로 **빨갛게 갈라 세웠던** 알약이다. 새 색 없이 `red` → `hot` 한 단어 — 이제 07장 알약 줄이 한 색으로 이어진다 |

### PM 안전 판 — 말투 10곳

「학생들은 어떻게 프롬프트해야 하는가?」 → **「AI에게 어떻게 물어야 하는가?」**,
「학생 수준을 높이는 비판적 분석 프롬프트」 → **「한 단계 더 — 비판적 분석 프롬프트」**,
「…넣어주는 것이 좋습니다. 특히 이번 수업이라면」 → 「…넣어야 합니다. 특히 이 사례라면」 등.
이번에도 `학생들에게는 <b>5가지…` 처럼 **태그가 낀 한 곳이 첫 치환에서 0건**이었다 — 28절과 같은 함정.

### 커밋이 둘로 쪼개졌다 — `git add` 사이에 남이 커밋했다

`git add 페이지 새파일` → `git commit` 사이 몇 초에 다른 세션이 **`21a2d6e`(KOSIS 프롬프트 알약 노랑)** 를 커밋하며
**내 페이지 수정(알약 · 설명 · `a.ylw` CSS)까지 파일째 가져갔다.** 내 `1d773c0` 에는 새 파일만 남았다.
결과물은 온전하다 — HEAD 페이지에 링크가 있고 파일도 있다. 다만 **커밋 메시지와 내용이 어긋난다**(`21a2d6e` 에 PM 알약이 들어 있음).

> **교훈 — 여러 세션이 같은 저장소 · 같은 인덱스를 쓴다.** 내가 `add` 한 것을 남의 `commit` 이 가져갈 수 있다.
> 커밋 뒤 `git show HEAD --stat` 이 예상과 다르면, **`git log -3 -- 그파일`** 로 내 변경이 어느 커밋에 들어갔는지 찾는다.

### 최종결과물 알약 — 이번엔 처음부터 줄 단위로

`tourism-venture.html` 에 한지 카드 작업이 미커밋으로 있어 `git pull` 이 막혔다.
HEAD 본에 CSS 두 줄 치환만 → `hash-object` → `update-index` 로 **2줄만** 커밋했다(28절 교훈 적용).

---
## 31. **예비창업패키지 — 자료 넷을 모으고, 내 실수를 하나 잡았다** (`8dba1b9` ~ `d2c1612`)

사용자가 URL과 파일을 차례로 던졌고, 05장 사분면의 **② 범부처 칸**에 쌓였다.

| 순서 | 무엇 | 커밋 |
|---|---|---|
| 1 | 창업중심대학 예비창업자 **사업계획서 양식(2024)** HWPX | `8dba1b9` |
| 2 | **2026 예비창업패키지 공고**(중기부 제2026-207호) | `800529b` |
| 3 | 회차 변경점 **사설 정리**(네이버 블로그) | `2d1259f` |
| 4 | **2026 사업계획서 양식** DOCX | `0d4abfc` |
| 5 | **PSST 쓰는 법** 사설 정리(패스해커) | `cf5e1e3` |

### ⚠️ 내가 틀린 것 — 붉은 「지금 열림」

2번을 **붉은 `.call.now`** 로 넣었다. **지금 열려 있는 공고라는 뜻의 색**이다.
그런데 3번 블로그를 열어 보니 **접수가 2026. 3. 6. ~ 3. 24. 16:00 으로 이미 끝나** 있었다.

```
공고 본문에는 접수 기간이 없다 → 첨부 hwpx · PDF 에만 있다
→ WebFetch 로 본문만 읽고 「지금 열림」으로 단정했다
```

**9/14 규칙 「공모 링크를 받으면 먼저 마감일을 본다」를 내가 어겼다.**
알약 이름에 **마감일을 박고**(「2026 예비창업패키지 공고 (3/24 마감)」) 색을 지난 회차로 되돌렸다.
**사설 블로그가 공식 공고보다 정확한 날짜를 갖고 있었던 셈이다.**

> **고친 규칙** — 공고 본문에 접수 기간이 없으면 **「지금 열림」으로 칠하지 않는다.**
> 첨부를 열 수 없으면 **마감일 미상**으로 두거나, 다른 출처로 교차 확인한다.

### 사설 글을 거는 법

3 · 5번은 둘 다 **끝에 유료 컨설팅을 파는 홍보 글**이다. 내용은 쓸 만하다 —
회차 변경점(분량 **15쪽 → 10쪽**, 자금 평균 4천만원)과 칸별 작성 요령이 잘 정리돼 있다.

**흰 바탕 점선 알약**(`.note`)으로 공식 공고와 격을 갈랐고, 설명에
**「읽되 사지 마십시오. 기준은 언제나 공식 공고와 양식입니다.」**를 박았다.

> **알약 모양만으로는 전달되지 않는다.** 학생이 보는 페이지에 상업적 유도가 있는 링크를
> 걸 때는 **그 성격을 글로 밝혀야** 한다.

### 칸 안이 터져서 밖으로 뺐다 (`d2c1612`)

자료가 다섯이 되자 사분면 **c3 칸 안에 문단 넷**이 들어차 **알약 줄 자체가 안 읽혔다.**
그림은 「어디에 내는가」를 한눈에 보여 주는 것인데 그 안에 긴 글이 들어가면 **그림이 표가 된다.**

칸에는 한 줄만 남기고, 사분면 「읽는 법」 **바로 아래**로 내려 머리줄 하나와 **열 칸**으로 폈다.

```
1) 어떤 사업인가  2) 이미 끝난 회차  3) 받는 것        ← 공고
4) 양식 10쪽      5) 구성은 PSST     6) 부처가 달라도  ← 양식
7) P는 증빙으로   8) S·S·T는 무엇으로 9) 쓰지 말 말     ← 쓰는 법
10) 사설 정리 둘                                      ← 출처 주의
```

머리줄은 **파랑** — 사분면에서 ② 범부처 칸이 쓰는 색이라 **그림의 어느 칸을 푼 것인지**가 색으로 이어진다.

**PSST 네 칸에 이 수업이 어디로 들어가는지**도 적었다 —
02장 문제 정의가 **1 Problem**, 03장 비즈니스 모델이 **3 Scale-up 의 「수익화 모델」** 칸.

---

## 32. **알약 색이 뜻을 갖기 시작했다** (`089d6e1` 외)

오늘 색을 바꾼 알약이 많았다. 되돌아보니 **규칙이 생겨 있다.**

| 색 | 뜻 | 예 |
|---|---|---|
| 🟢 밝은 초록 `#1faa59` | **같은 자료의 다른 창구 · 판** | KOSIS 시각화 둘 · 데이터랩 알약 넷 · 캠퍼스 수상작 · **BSP TOP10 두 판** |
| 🟡 노랑 | **내려받아 채우는 서식 · 프롬프트** | 사업계획서 예시 HWP · AI 프롬프트 워크북 · 해석 프롬프트 |
| 🔴 빨강 | **최종결과물 · 지금 열린 공고** | 사업계획서 한 부 |
| 🟤 고동 | **지난 회차 개별 공고** | 창업중심대학(2024) · 예창패(3/24 마감) |
| ⌐ 흰 점선 | **공식이 아닌 정리 글** | 사설 정리 둘 |

> **처음부터 정한 규칙이 아니다.** 사용자가 「이건 노란색」 「이건 밝은 녹색」이라고
> 하나씩 고쳐 온 것이 쌓여 뜻이 생겼다. **이제부터는 새 알약을 넣을 때 이 표를 먼저 본다.**

---

## 33. **한지 8+1 · 정부 R&D 두 카드 · 안동온사람들로 뛰는 알약** (`80ed04c` ~ `d8a9723`, 21-2절 세션)

### 한지를 로컬크리에이터 12장에 (`80ed04c` · `f7f166f` · `23998fc`)

`한지_깊이이해_8plus1_핵심지식_날짜포함.html` — 사이트에 **한지를 다루는 자리가 없었다.**
세 자리(12장 로컬크리에이터 · 스마트투어리즘 안동 · 목차 독립)를 물었고 **12장**을 받았다.

| | |
|---|---|
| 파일 | `learn/hanji-8plus1-deep-knowledge.html` — 내용 그대로, 오른쪽 위 날짜 「2026년 9월 14일」 유지 |
| 자리 | 12장 끝 보라 카드 셋째 — 소상공인 교재 · 스티키 보드 **다음** |
| 왜 거기 | 위 둘이 **정책 · 회사를 넓게**, 한지는 **자원 하나를 끝까지** — 넓게 본 다음 깊게 |
| 설명의 연결 | 끝의 가치사슬(자원 · 기술 · 장인 · 제품 · 체험 · 관광 · 산업)이 **7대 유형의 지역기반제조 · 지역특화관광**이 서는 자리 |

말투 두 곳을 사용자 결정으로 고쳤다 — 맨 아래 「강의/연구용」 → **「예시용」**,
10절 제목 「연구·강의용으로 공부한다면」 → **「내 지역 자원으로 따라 해 본다면」**.
10절 안 「연구 관점에서는 … 좁혀가면」은 **방법을 말하는 문장**이라 남겼다.

> **이번에는 `git add 두 파일` → `git diff --cached --stat` 로 322줄(317 + 5)만 들어간 것을 보고 커밋했다.**
> 30절의 「남의 미커밋이 딸려 간」 사고를 한 번 더 겪지 않으려고.

### 정부 R&D 두 카드를 한 줄에 (`8fb5718`)

같은 목록(2열 격자 · 카드 아홉)에서 「**한 단계 위 · 정부 R&D**」(9번, 혼자)와 「**예고와 보도자료**」(6번)가 멀리 떨어져 있었다.
**카드는 그대로, 순서만** 바꿨다(24줄 삭제 · 24줄 추가).

```
전                                        후
5 부산관광스타트업 │ 6 R&D 예고와 보도자료     5 부산관광스타트업 │ 6 관광데이터 활용 공모전
7 생성형 AI 여정   │ 8 관광데이터 활용 공모전  7 한 단계 위 · 정부 R&D │ 8 R&D 예고와 보도자료
9 한 단계 위 (혼자)                          9 생성형 AI 여정 (혼자)
```

**「한 단계 위」의 첫 문장 「공모에는 층이 있습니다」가 바로 위 공모전 카드에서 이어진다** — 그래서 6번 자리에 공모전을 두었다.
아홉이라 한 칸은 혼자 설 수밖에 없고, **자체 제작 자료**를 맨 끝에 보냈다.

> 블록 사이 공백이 **정확히 빈 문자열인지 assert** 한 뒤 재조립했다(19절 막힌 곳 ②의 반대로 — 가정하지 말고 검사).

### 「유형은 대개 셋」에서 안동온사람들로 (`d8a9723`)

01장의 세 유형(시설·공간형 · 플랫폼·중개형 · 콘텐츠·데이터형) **바로 앞 문장 끝**에
파란 알약 「**실제 회사로 보기 · (주)안동온사람들 ↓**」를 붙였다. 파트 04 의 접힌 상자로 뛴다.

| 한 것 | 왜 |
|---|---|
| 접기에 `id="안동온사람들"` | 앵커 |
| `a.jump-in` 파랑 `#2563a8` | 이 페이지의 **같은 페이지 안으로 뛰는 알약** 색(`q-lk a.jump`)과 같게 |
| 작은 스크립트 — `#id` 가 `<details>` 면 `open = true` | 닫힌 제목 한 줄에 도착하면 헛걸음이다. `hashchange` + 첫 로드 둘 다 |
| `details:target { scroll-margin-top: 78px }` | 고정 머리에 제목이 가리지 않게 |

> **남은 틈** — 한 번 열고 직접 닫은 뒤 **같은 알약을 다시 누르면** 주소가 같아 `hashchange` 가 안 뜨고 다시 열리지 않는다.
> 필요하면 알약 `click` 에도 걸면 된다. 사용자에게 알렸다.

### 이 세션의 푸시가 남의 커밋을 함께 올렸다

「커밋 푸쉬 배포」를 받고 보니 내 커밋은 이미 올라가 있었고, **다른 세션의 `5eaff2c`**(워크북 알약 노랑)가
로컬에만 있었다. `rebase --autostash` 후 **함께 푸시했다** — 같은 저장소 · 같은 사용자 요청이라 올리는 쪽을 택했고, 사용자에게 밝혔다.

---

## 34. **저녁 (16:58 ~ 17:53) — 색 한 장씩, 받은 파일 셋은 이미 있었고, 격자 둘을 고쳤다** (16절 세션)

### 색 · 모양 — 「여기서 ○○만」

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `9d749ad` · `5eaff2c` | 07장 **② 여행사 사업계획서 예시 HWP**(빨강 → 노랑) · **AI 프롬프트 워크북**(초록 → 노랑) | `tv-travel a.mapl.form.ylw` 신설. 32절 표의 🟡 행이 이 둘에서 시작됐다 |
| `16ee6ed` | 정부 R&D 두 카드 — **연한 남색(`t-navy`) + 연한 회청(`t-slate`)** | 사용자가 「보라 촌스러워」 → 세 안(파랑 두 단계 · 청록 · 남색+회청) 중 **남색+회청, 좀 연하게**. 새 토큰 둘, 두 카드에만 |
| `51da8f1` | 창업 지원 ② 칸 **2026 예비창업패키지 양식 DOCX → 빨강**(`q-lk a.dl.red`) | 옆 창업중심대학(2024) HWPX 는 초록 그대로 |
| `2ff2080` | 과제 1 알약 줄 **아래 한 줄**에 같은 DOCX 를 **연한 빨강**(`#e0665a`, `tb-tags.sub` · `red.soft`) | 윗줄 공식 HWP(진한 빨강)보다 한 단계 아래. 옆 설명 한 줄은 **내가 쓴 문장** — PSST 가 HWP 와 「닮았다」로 **낮춰** 썼다(「거의 겹친다」는 확인 못 함) |
| `b6738ec` | 07장 **여행업 기업 수익모델 · BSP 통합 시각화 → 밝은 초록**(`hot.bright`) | 32절 🟢 행과 같은 `#1faa59` |
| `7add9f6` | 린 스타트업 삼각형 **「묻는다 · 고객 검증」만 주황 꽉 참**(`li.or-solid`) | 사용자가 도중에 「스톱 묻는다 부분 삼각형만」「배운다는 내버려둬」 |
| `937b230` | 비즈니스 모델 삼각형 **「고객 · 채널」만 진한 청록 꽉 참**(`li.cy-solid`) | 「이부분이 중요하다고 생각해서」. 색은 그 삼각형의 청록 계열을 따랐다 |
| `173d690` | 스마트투어리즘 로드맵 **최종결과물 1 · 2 꼬리표 → 샛노랑 둥근 알약**(`rm-task b.fin`) | 실습 1 · 2 꼬리표는 빨강 그대로 |
| `6601bc3` · `37cd5c5` | 학사일정 상자 제목 → **「2학기 학사일정 달력 ↗」**(빅데이터 · 관광벤처) | 스마트투어리즘이 이미 쓰던 이름. 세 과목이 같아졌다 |
| `62a2b09` | 「SPSS 사용 가능」 → **「SPSS 가능」** | |
| `c4b976d` | **교육자료 목차 소개문** 한 문장으로 · 「수업을 듣지 않아도 열립니다」 삭제 · **Noto Sans KR** | `.learn-hero .sub` 는 모든 교육자료 페이지가 함께 쓴다 → `learn.css` 를 고치지 않고 **목차 페이지에만** `.sub.ko` |

> ⚠️ **32절 색 표와 어긋나는 한 칸.** 표는 🔴 빨강 = 「최종결과물 · 지금 열린 공고」, 🟡 노랑 = 「내려받아 채우는 서식」인데,
> 사용자가 **예비창업패키지 양식 DOCX 는 빨강**을 골랐다. 이 페이지의 오래된 규칙(「**그대로 내는 공식 서식은 빨강**」 — 공식 HWP)과는 맞는다.
> 표에 한 줄 더: **🔴 그대로 내는 공식 양식 · 🟡 채우며 연습하는 예시 · 워크북.**

### 받은 파일 셋 — 모두 저장소에 이미 있었다

| 받은 파일 | 저장소 | 한 일 |
|---|---|---|
| `스마트투어리즘_15주_강의계획서_인터랙티브 (1)` | `smart-tourism-syllabus-15w-interactive.html` | 해시 같음 → 자리(2안 칸)만 알렸다 |
| `스마트관광도시_12곳_…_분석프롬프트20_통합 (2)` | `smart-tourism-cities-funding-prompts.html` | 해시 같음 → **공식 사이트 카드 하단에 알약 하나 더**(`5a30c15`). 지도 밑 줄과 **문이 둘** |
| `여행업_기업_수익모델_BSP_통합시각화 (2)` | `travel-agency-revenue-model-bsp.html` | 해시 같음 → **색만** 밝은 초록 |

> **다운로드 폴더의 `(1)` `(2)` 는 대부분 같은 파일을 다시 받은 것이다.** 오늘 세 번 다 그랬다.
> 올리기 전에 해시로 대조하면 중복 파일 · 중복 알약을 만들지 않는다.

### 새로 올린 것 둘 — 말투 21곳

| 커밋 | 파일 | 자리 | 말투 |
|---|---|---|---|
| `6ca2eed` | `chatgpt-job-prompts.html`(ChatGPT 취업 활용 프롬프트 6) | **세 과목** 채용 창구 줄, 두바이 인턴십 옆 **밝은 초록**(`job-more a.mapl.lime`) | 12곳 — 「★ 수업 포인트」 6 → 「★ 여기가 핵심」, 「순서를 가르칩니다」 → 「순서로 갑니다」 등 |
| `3e3181a` | `inbound-travel-companies-cases.html`(인바운드 기업 다섯 · 7.8MB) | 07장 인바운드 18섹션 옆 | 9곳 — 「수업 핵심」 5 → 「핵심」, 「학생 적용 예시」 → 「내 아이템에 대 보기」 등 |

### 고친 버그 · 레이아웃

| 커밋 | 무엇 | 원인 · 방법 |
|---|---|---|
| `65bf392` | BSP 표 **머리글이 1위 줄을 덮음** | `th{position:sticky;top:54px}` — 페이지 상단 바 밑에 붙이려던 값인데 표가 `overflow:auto` 상자 안이라 **상자 기준 54px 아래**에 섰다. sticky 제거. 같은 규칙은 이 파일 하나뿐임을 전수 확인 |
| `ceb40ec` | ChatGPT 프롬프트 6장 **2×3 격자 → 좌우 한 줄** | 격자 `repeat(6,100vw)` × 한 행, `positions` 여섯을 y=0 으로, ↑↓ 버튼 `hidden`, 세로 스와이프 · ↑↓ 키 제거(긴 장 안 스크롤로 돌려줌). `node --check` 통과 |
| `a5e30f8` | 로드맵 「…업무수행계획서 **모의 PT**」 | `rm-when.br` — 다음 줄 · `nowrap` 으로 「모의 / PT」 쪼개짐 방지 |
| `5a69900` | 관련 링크 그리드 앞머리 | **K-MOOC │ 공공데이터포털**, 그 아래 **한국관광 데이터랩을 전체 폭**(`li.wide`). 전체 폭 둘 + 나머지 열둘 → 혼자 서는 카드 없음 |
| `399e6e7` | 「학생 이해 노트 ↗ · 공식 출석부 12명」 | 강의실 바 밑 → **학사일정 달력 상자 맨 아래** |

### 확인만 한 것

- **취업 상자 세 과목 비교** — 사용자가 빅데이터 쪽 알약 목록을 주며 「스마트 · 벤처에도 동일하게, 한쪽에만 있으면 알려 달라」.
  공통 알약은 **이미 셋 다 같았다.** 스마트투어리즘에만 **「학생 지원 한자리」 · 「K-MICE 채용」**, 관광벤처에만 제목 앞 **「과제 2」 배지**. 둘에도 넣을지 물어 둠.

---
## 35. **밤 (17:59 ~ 18:24) — MICE 창구 둘, 삼각형 색 정리, 좌우 넘김 하나, 교수자 말투 33곳** (16절 세션)

| 커밋 | 무엇 | 메모 |
|---|---|---|
| `a6940b9` | 로드맵 최종결과물 2 의 **「모의 PT」를 글 첫 줄에 맞춰 들이고 노란 글자** | `rm-task.tg` 를 `grid(auto 1fr)` 로 — 꼬리표 폭만큼 둘째 줄이 들어간다. 글을 `span.tx` 로 감쌌다 |
| `f77d66e` | 스마트투어리즘 · 관광벤처 채용 줄 **관광in 옆 K-MICE 채용 → 연한 빨강**(`job-more a.mapl.soft`, `#e0665a`) | 주소를 열어 **「정규/계약직 채용」 게시판**임을 확인. 스마트투어리즘은 있던 갈색 알약을 **색만**, 관광벤처는 **새로** |
| `3d98441` | 그 옆에 **PROMICE · 희망MICE인턴십** | 첫 화면이 스크립트로만 그려져 소개 글이 안 읽혔다 → **검색으로** 한국PCO협회 희망MICE인턴십 지원 플랫폼임을 확인하고 이름을 붙였다 |
| `cebb377` | 린 스타트업 삼각형 **세 카드 하늘색 통일**, 가운데 **「묻는다」만 진한 하늘**(`li.sky-solid`, `#1f7fb8`) | 30분 전 `7add9f6` 에서 주황으로 세웠던 카드다. 「배운다」 초록 → 연한 하늘. `or-solid` CSS 는 쓰는 곳 없이 남음 |
| `e791aa8` | 01장 **「실제 회사로 보기 · (주)안동온사람들 ↓」 → 밝은 초록** | `a.jump-in` 이 페이지에 하나뿐이라 규칙 색을 직접 바꿨다 |
| `20825d5` | 교육자료 목차 소개문 **두 줄** | `max-width 620 → 880px` · 14.5px · 「열두 모듈이며,」 뒤 `br.br-d`. **900px 아래에서는 `br` 을 숨겨** 자연스럽게 흐르게 |
| `253928d` | 데이터랩 **PM 안전사례 판 → 일곱 장 좌우 넘김** | 아래 |
| `16a392c` | ADsP 안내 카드에 **「ADsP를 취업으로 잇는 법」** 알약(`adsp-career-link.html`) | 아래 |
| `3aa8a8b` | 6차산업 삼각형 **「3차 · 체험 · 관광」 → 밝은 초록 꽉 참**(`li.lime-solid`) | |

### PM 안전사례 — 「손실 없이」 좌우 넘김으로 (`253928d`)

일곱 `section.card` 를 **그대로 두고** 트랙(`deckt`) 하나로 감쌌다. 마크업은 옮기지도 고치지도 않았다.

```
.js .deckv  overflow:hidden · height 를 지금 장 높이에 맞춤(장마다 길이가 다르다)
.js .deckt  display:flex · translateX(-100% × i)
```

- **넘김** — 위 · 아래 「← 이전 · n / 7 · 다음 →」, ← → 키, 좌우 스와이프(세로보다 1.5배 클 때만)
- **원래 목차 알약 일곱** → 누르면 그 장으로, 지금 장은 남색
- **주소 `#dikw`** 로 그 장부터 열림 · `replaceState` 로 넘길 때 갱신
- **스크립트가 없으면** `.js` 클래스가 안 붙어 **예전처럼 세로로 모두 보인다**

> **「손실 없이」를 어떻게 확인했나** — 고치기 전(HEAD) · 후 파일에서 스크립트 · 스타일 · 넘김 막대를 걷어 낸 **글자만** 뽑아 비교했다. **4,053자 = 4,053자, 완전히 같다.**
> 눈으로 훑는 대신 이렇게 대조하면 「한 칸 빠졌나」를 기계적으로 답할 수 있다.

### ADsP 판 — 처음부터 끝까지 교수자에게 하는 말이었다 (`16a392c`)

`ADsP_관광빅데이터분석_취업연계_대화전체.html` 은 **ChatGPT 가 교수자에게 한 답**을 그대로 저장한 파일이었다 —
「좋습니다.」로 시작해 「선생님 수업 방향은 꽤 좋습니다」 「학생들에게 이렇게 가르치면 좋습니다」 「지도하시면 좋습니다」.
몇 곳이 아니라 **문서 전체의 화자가 교수자 쪽**이어서, 올리기 전에 물었다(원문 그대로 / 교수자용 표시 / 학생 말투).
사용자가 **학생 말투**를 골랐다.

| 전 | 후 |
|---|---|
| 관광빅데이터분석 수업 **취업연계 대화 전체 정리** | 관광빅데이터분석 **ADsP를 취업으로 잇는 법** |
| 이 채팅에서 제시한 내용을 한 파일에 통합했습니다 | 내 이력서에 옮겨 쓰는 순서로 한 파일에 모았습니다 |
| 선생님 수업 방향은 … 좋습니다. 다만 학생들에게 「ADsP 취득 = 취업」이라고 가르치기보다는 | 다만 「ADsP 취득 = 취업」은 아닙니다 |
| 학생들에게 분명하게 알려주시는 게 좋습니다 | 분명히 알아 두십시오 |
| …보이도록 지도하시면 좋습니다 | …보이게 하십시오 |
| 학생 한 명마다 … 완성하도록 합니다 | … 완성합니다 |

**33곳**, 고친 뒤 「선생님 · 학생 · 지도 · 가르치 · 시키 · 채팅 · 대화」를 다시 훑어 **0건**.
이력서 문구 · 지원 분야별 7종 · 자소서 500자 · 면접 40초 · 표는 **손대지 않았다.**

> **막힌 곳** — PowerShell 문자열 안의 **둥근 작은따옴표 `‘ ’`** 를 PowerShell 이 **문자열 끝**으로 읽어 스크립트가 통째로 죽었다.
> 치환 목록을 **파이썬 파일**(스크래치패드)로 옮겨 돌렸다. 치환마다 **정확히 1건 맞는지** 세도록 했고 누락 0.

---

## 36. **IR 피칭 칸에 영상 둘 — 「어디」를 짐작했다** (`231afa3` · `62210e5`, 16절 세션)

사용자가 유튜브 링크 둘을 주며 **「창업 IR 스피치 아래 하단에」** 넣어 달라 했다.

| 커밋 | 영상 | 넣은 모양 |
|---|---|---|
| `231afa3` | **「아이디어로 창업하지 말라 │ 정세주 눔 대표」** — World Knowledge Forum **쇼츠** | 세로 9:16 · 폭 240px(`q-cell .ir-short`) |
| `62210e5` | **「2025 정주영 창업경진대회 · 트랙별 피칭 모아보기」** — 아산나눔재단 | 가로 16:9 · 칸 폭(`.ir-short.wide`), **`start=12173`(3:22:53)** |

**제목은 oEmbed 로 확인했다** — `youtube.com/oembed?url=…&format=json`. 링크만으로는 제목이 없어서 설명을 지어낼 뻔한 자리다.
설명 한 줄씩은 **내가 쓴 문장**이다(쇼츠 = 「무엇에서 출발하나」, 피칭 = 「무대에서 몇 분 안에 말하는 모습」).
공유 링크의 `t=12173` 은 임베드에서 `?start=12173` 으로 옮겨야 같은 자리에서 시작한다.

### 「창업 IR 스피치」라는 절이 없었다

페이지를 뒤져 가까운 후보가 둘이었다.

| 후보 | 근거 |
|---|---|
| **창업 지원 네 칸 중 「사업계획서 · IR 피칭 배우기」** ← 골랐다 | 이름에 IR 피칭, 칸 안에 **티엔티스피치** 강의 |
| 08장 외식 창업의 **성심당 영상** | 캡션이 「IR티켓_**티엔티스피치**」 |

영상 내용(아이디어보다 문제 · 창업 피칭)이 외식보다 IR 쪽이라 앞을 골랐고, **보고에 다른 후보를 적어 옮길 수 있게** 했다.

> **교훈 — 사용자가 부른 이름이 페이지 제목과 다르면, 가장 가까운 자리를 고르되 두 번째 후보를 보고에 남긴다.**
> 매번 묻는 것보다 빠르고, 틀렸을 때 한 번에 옮길 수 있다.

### 두 번 다 줄 단위 커밋

`tourism-venture.html` 에 다른 세션의 **여행업 창업전략 알약**(`7be93c8` 로 뒤에 올라감)이 미커밋으로 있었다.
파이썬으로 **작업 트리와 HEAD 본에 같은 삽입**을 하고 HEAD 본만 `hash-object` → `update-index` — 5줄 · 3줄만 들어갔고,
커밋의 부모가 직전 HEAD(`de8219c` · `231afa3`)인 것을 확인했다.

---

## 오늘의 규칙

- **같은 자리를 세 번 고치면 방법이 아니라 그 요소를 의심한다.** 빼는 선택지를 먼저 내민다.
- **남의 커밋을 받은 뒤에는 내 편집 대상이 아직 있는지 grep 한다.** 없으면 CSS 도 같이 걷는다.
- **자료를 걸기 전에 말투를 grep 한다** — 오늘도 한 줄이 걸렸다.
- **알약을 한 줄에 더할 때는 기존 알약의 `margin`·`align-self` 를 먼저 본다.**
  `margin-top:auto` 가 알약에 붙어 있으면 둘이 나란히 서지 않는다.
- **`<li>` 를 셀 때 `class` 붙은 변형을 함께 센다.** 다른 세션이 `li.wide` 로 바꿔 두면 개수가 어긋난다.
- **진행 출력에 em dash · 화살표를 넣지 않는다.** cp949 콘솔에서 죽고, 쓰기 전이면 작업이 통째로 날아간다.
- **「안 보인다」는 말을 들으면 라이브를 직접 받아 확인한다.** 내가 안 넣은 것인지 캐시인지 갈라야 한다.
- **같은 기관의 다른 창구는 카드 안 알약으로 넣는다.** 별도 카드로 빼면 「또 다른 사이트」로 읽힌다.
- **분석 도구를 소개하면 그 도구로 쓴 논문을 옆에 붙인다.** 도구만 보면 「그래서 뭘 하나」에서 멈춘다.
- **결과와 과정은 다른 자료다.** 영상 · 사례집은 결과, 연구노트 · 인터뷰는 과정 — 학생에게 필요한 쪽은 뒤다.
- **공고 본문에 접수 기간이 없으면 「지금 열림」으로 칠하지 않는다.** 첨부에만 있는 경우가 많다.
  교차 확인이 안 되면 마감일 미상으로 둔다 — 오늘 이걸 어겨 끝난 공고를 붉게 칠했다.
- **상업적 유도가 있는 링크는 그 성격을 글로 밝힌다.** 알약 모양만으로는 전달되지 않는다.
- **알약 색은 뜻이다.** 초록=같은 자료의 다른 판, 노랑=채우는 서식, 빨강=최종결과물,
  고동=지난 공고, 흰 점선=비공식. 새 알약은 이 표를 보고 고른다.
- **공모 링크를 받으면 먼저 마감일을 본다.** 지났으면 「지금 낼 곳」이 아니라 「내년 대비」가 자리다.
- **요강과 수상작은 다른 자료다.** 요강은 무엇을 내라, 수상작은 어느 수준으로 내라 — 학생에게 급한 쪽은 뒤다.
- **공고에 없는 항목은 없다고 적는다.** 부문·상금·일정이 본문에 없으면 「요강 PDF 확인」을 못박는다.
  그럴듯하게 채우면 학생이 잘못 준비한다.
- **작업일지를 이어 붙이기 전에 그 파일의 마지막 절 번호를 읽는다.** 오늘 두 파일에서 번호가 겹쳤다.
- **격자에 항목을 넣을 때는 칸 수와 항목 수를 센다.** 맞지 않으면 `grid-column`·`grid-row` 를
  항목마다 못박는다. 태그 검사도 링크 검사도 이건 못 잡는다 — **화면을 봐야 보인다.**
- **편집 스크립트는 줄바꿈(CRLF/LF)을 감지해 원래 형식으로 되돌려 쓴다.** 이번 주에 두 번 걸렸다.
- **내용을 다른 주차로 옮기면 시간 낱말(「지난주」 「이날은」)과 접힌 제목 꼬리를 같이 고친다.**
- **줄 단위 커밋 뒤에는 `git show HEAD --stat` 줄 수를 본다** — 그 사이 남의 커밋을 되돌리지 않았는지.
- **커밋 전 `git diff --stat` 줄 수가 내 예상보다 크면 파일째 add 하지 않는다.** `??` 로 남의 새 파일이 보이면 더더욱.
- **지난 공고 링크는 이름에 연도를 붙인다** — 열어서 게시일부터 본다.
- **「여기서 ○○만」은 형제에 번지지 말라는 뜻이다** — 같은 모양의 형제 전부를 바꾸는 9/13 규칙의 예외.
- **다운로드 폴더의 `(1)` `(2)` 는 올리기 전에 해시로 저장소와 대조한다** — 오늘 세 번 모두 같은 파일이었다.
- **여러 페이지가 함께 쓰는 선택자(`.learn-hero .sub` 등)는 공용 CSS 를 고치지 말고 그 페이지에만 변형 클래스를 붙인다.**
- **`position: sticky` 는 스크롤 상자(`overflow:auto`) 안에서는 그 상자 기준이다** — 페이지 상단 바 높이를 `top` 에 넣으면 내용을 덮는다.
- **「손실 없이」 바꿨는지는 전 · 후 파일의 글자만 뽑아 대조한다** — 눈으로 훑지 않는다.
- **받은 문서의 화자가 통째로 교수자 쪽이면 고치기 전에 묻는다** — 몇 곳 고치는 일과 문서 성격을 바꾸는 일은 다르다.
- **한글 문서 치환은 PowerShell 대신 파이썬 파일로** — `‘ ’` 같은 둥근 따옴표가 PowerShell 문자열을 끊는다.
- **유튜브 링크는 oEmbed 로 제목부터 확인하고, 공유 링크의 `t=` 는 임베드 `?start=` 로 옮긴다.**
- **블록을 옮기면 남는 소개 문단을 본다** — 지울 때 출처는 옮겨 살린다.
- **색 · 모양 요청은 같은 페이지의 기존 변형 클래스부터 찾는다.**

---

## 남은 것

- ⚠️ **경북 AX 경진대회 — 9/15(화) 18:00 마감. 내일이다. 3주차 수업 당일이다.**
  **수업에서 안내하면 늦는다.**
- ⚠️ **부산 빅데이터 활용 대회 — 9/18(금) 17:00 마감.** 나흘 남았다.
- ⚠️ **AI 배리어프리 공고(관광공사) — 9/16(수) 11:00 마감.** 알약에 **「기업 대상」 표기가 없다** —
  예비창업자인 학생이 낼 수 있는 공고로 읽힐 수 있다. 한 줄 붙일지 물었으나 답을 못 받았다.
- **평가방식 — 실제로 어느 안을 쓰는지** 아직 페이지에 없다. 두 안을 나란히 보여 줄 뿐이다.
- **2안 문서의 운영 방식이 페이지와 다르다** — 2안은 「주 2회 · 회당 75분 · 30회차」,
  페이지는 「수 100분 + 목 50분」.
- **AI 프롬프트 워크북 저장 기능** — 미정. 지금은 PDF 로 남기라는 안내만 있다.
- **과제 4 이름이 자리마다 다르다** — 로드맵 「지역문화재단 · 관광공사」 ↔ 상자 「지역관광재단 연구원」.
- **로드맵 도해에 파트 04 가 없다**(스마트 투어리즘).
- **히어로에 「2학년 2학기」·「4학년 2학기」가 두 번씩.**
- **삼각형 점선** — 옅게 하자고 제안했으나 답을 못 받았다.
- ~~**관광빅데이터분석 첫 카드 그리드가 열셋(홀)**~~ — **21절에서 해결.** 플랫폼 칸을 두 칸 폭으로 세워 나머지 열둘이 짝이 맞는다.
- **관광빅데이터분석 07절 「빅데이터 공모전」 아래 첫 문단이 부산 사례** — 제목과 어긋난다. 옮기거나 바꿀지 미정.
- **SPSS 사용 가능** — 스마트투어리즘 · 관광벤처 강의실 바의 2106호에도 넣을지 미정.
- **취업 상자** — 「학생 지원 한자리」 · 「K-MICE 채용」을 관광빅데이터분석 · 관광벤처창업론에도 넣을지 미정.
- **정부 R&D 두 카드(연한 남색 · 회청)** — 너무 흐리면 한 단계씩 진하게. 화면 확인 전.
- **인바운드 기업 사례판 7.8MB** — 휴대폰 첫 로딩. 느리면 그림을 webp 로 뽑아 줄일 것.
- **린 스타트업 삼각형** — 30분 사이 주황 → 하늘로 바뀌어 `or-solid` CSS 가 쓰는 곳 없이 남아 있다. 걷어낼 것.
- **K-MICE · PROMICE** — 관광빅데이터분석 채용 줄에는 넣지 않았다(요청이 스마트 · 벤처). 넣을지 미정.
- 호텔인네트워크 · hosco · Glassdoor 세 채용 링크는 **여전히 봇 차단으로 확인하지 못했다.**

> **내일이 3주차 9/15(화) 「아이디어는 문제에서 나온다」.**
> 워크북 **시트 1 · 2** 를 채우는 날이고(시트 1 을 2주차에서 옮겨 왔다), **그날 18시가 AX 경진대회 마감**이다.
> 9/16 발표 배정(울산 · 수원 · 대구 · 인제 · 경주 · 남원 · 용인)도 그날 확인한다.
