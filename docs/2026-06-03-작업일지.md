# 2026-06-01 ~ 06-03 작업일지

## 선샤인 웰니스 — 다국어(ZH/EN) 완성 · 교육자료(/learn) 허브 신설

---

## 1. Where to Next? 게시판 다국어 완성

### 게시판 목록·작성자·상세 다국어 (KO/EN/ZH)
- `_boardListI18n` 맵: 8개 글의 제목·여행지·작성자 EN/ZH 번역
- `_openBoardDetailInOverlay()` 다국어화 — 글 클릭 시 제목·본문·작성자·라벨이 해당 언어로 표시
  - 8개 글 **본문(content)** 전체를 ZH·EN로 번역
- `boardSearch()` 검색을 현재 언어 번역 제목 기준으로 필터링

---

## 2. 서브페이지 오버레이 열린 상태에서 언어 전환 즉시 반영

- **문제**: 서브페이지는 열릴 때의 언어로 한 번만 그려져, 오버레이가 떠 있는 동안 언어 버튼을 눌러도 안 바뀜
- **해결**: 전역 `_activeSubPageRender` 도입
  - 각 `open***Page()` 함수가 자기 자신(인자 있으면 클로저)을 등록
  - `setLang()`에서 오버레이가 열려 있으면 그 함수를 현재 언어로 재호출
  - `closeSubPage()`에서 초기화
- 적용 11개 서브페이지 전체
- **부작용(의도된 트레이드오프)**: 재렌더이므로 언어 전환 시 페이지가 초기 상태로 그려짐(게시판 페이지·검색·폼 입력 초기화). 글 상세는 같은 글 유지

---

## 3. 아트 컨시어지 페이지 전체 ZH/EN 번역

### openArtConciergePage — 본문 전체 다국어화
- 함수 내 `L` 라벨 객체(`zh`/`en`/`ko`)로 분기, 템플릿의 모든 한국어를 `${L.키}`로 치환
  - 배너·제휴투어·대표소개·통계·철학문단·태그·언론/강의 카드·전문서비스 3개 섹션+투어·아카이브 소개·CTA
- 외부 링크 URL(부산일보·동서대·mice)·영문 라벨은 그대로 유지

### Cultural Archive 50개 사례 데이터 ZH/EN
- **핵심 원칙**: 원본 `culturalArchiveData.js`(한국어)는 **불변**. 번역은 별도 파일로 분리
  - `ART/culturalArchiveZh.js` → `_archiveZh = { no: {city,country,continent,caseName,concept,kw,topic} }`
  - `ART/culturalArchiveEn.js` → `_archiveEn = { ... }`
  - index.html에서 데이터 파일 다음에 로드
- `_buildArchiveCards(lang)`·`openCulturalCasePage()`에서 언어 분기 조회(없으면 원본 폴백)
- 유형명/설명은 인라인 맵(TZH/TEN/TYPE_ZH/TYPE_ENF), UI 문구(칼럼/원문보기/전체사례/출처) 번역
- 외부 칼럼 제목·URL은 원본 유지
- 검증: 50개 case no 전부 zh/en 맵에 존재(missing 0)

### openArtConciergeArchive (퀵 사이드바 "시그너처 문화공간")
- 별도 함수라 미번역 상태였음 → `AL` 라벨 객체(zh/en/ko)로 배너·뒤로가기·소개·Source·CTA 다국어화

---

## 4. 교육자료(/learn) 허브 신설 — 새 방향

### 배경·결정
- 사이트를 **영업용 + 교육용**(관광자원·해설·문화공간 재생·웹빌드)으로 확장
- **핵심 결정**: 교육 콘텐츠는 기존 SPA 오버레이가 아니라 **실제 정적 HTML 페이지 `/learn/`** 로 제작
  - 이유: 고유 URL·SEO·공유/인용 가능, script.js 비대화 방지, 페이지 단위 점진 다국어
- 포지셔닝: 영업 보조 신뢰도 레이어 / 언어: 한국어 우선

### 제작물
- `learn/index.html` — 교육 허브(4개 모듈 카드: 관광자원·해설·문화공간재생·웹빌드)
- `learn/learn.css` — 브랜드 일치 자체완결 스타일(메인 style.css와 독립)
- `learn/making-of.html` — **"이 사이트로 배우는 웹 만들기" 8강 교재**
  - 1강 SPA구조 · 2강 다국어 · 3강 상태·재렌더 · 4강 정보구조 · 5강 정적페이지 · 6강 SEO · 7강 배포 · 8강 협업
  - 각 강 = 「실제 과업 → 핵심 개념 → 교훈」, 전부 이 사이트의 실제 결정 기반
- `sitemap.xml` · `robots.txt` 신규(이전엔 없던 SEO 토대)
- nav에 "교육자료 / Learn / 学习" 항목 추가(데스크톱+모바일, `nav.learn` i18n)

### 모듈 연결 상태
| 모듈 | 상태 | 연결 |
|---|---|---|
| 관광 자원 | 준비 중 | 본문 미작성 |
| 해설 | 준비 중 | 본문 미작성 |
| 문화공간 재생 | 작동 | 기존 50개 아카이브 딥링크(`../#cultural-archive`) |
| 웹사이트 빌드 | 작동 | 내부 교재(`making-of.html`) + 외부 강의(`yuanlee.info/class.html`) |
- 딥링크 처리: `index.html`을 `#cultural-archive`로 진입 시 `openArtConciergeArchive()` 자동 오픈(DOMContentLoaded 해시 감지)

---

## 5. 인프라·운영 메모

### 듀얼 호스팅 + 게시판 CORS (타 터미널 작업)
- 도메인 `sunshinewellness.co.kr`은 **GitHub Pages**(정적, 서버함수 불가), 별도로 **Vercel** 배포 존재
- 게시판 저장/조회는 Vercel 함수(`/api/posts`)를 cross-origin 호출하도록 `BOARD_API` 상수 + CORS 헤더 적용
- 로고 클릭 → `goHome()`(서브페이지 닫고 최상단)

### 주의 — 두 터미널 동시 작업
- 이 기간 두 터미널이 동일 저장소를 만져 커밋이 섞이고(타 터미널이 stage해둔 변경이 다른 커밋에 휩쓸림), 린터 자동수정(CRLF) 충돌 발생
- **권장: 한 터미널에서만 작업**

---

## 커밋 이력 (주요)

| 커밋 | 내용 |
|---|---|
| `bdf6c42` | Where to Next 목록 제목·여행지 ZH/EN |
| `40e8cc9` | 게시판 작성자 이름 다국어 |
| `1acc4a1` | 게시판 상세 EN |
| `489b4e3` | 게시판 글 상세 CH 본문 |
| `cc6665d` | 오버레이 열린 상태 언어 전환 즉시 반영 |
| `5f8c0ca` / `ae1c342` | BEST Reviews·Community 테이블 다국어 |
| `3fa8686` | 아트 컨시어지 페이지 CH |
| `42058c0` | 아카이브 50개 사례 CH |
| `f9edd4c` | 아트 컨시어지 페이지 EN(+50개 사례 EN) |
| `e7a42b1` | 시그너처 문화공간 페이지 ZH/EN |
| `c76cc78` | 교육자료(/learn) 허브 신설 — Phase 1 |
| `32590ea` | 허브 소개문 수정(로컬 가이드 연결) |
| `5a86a06` | Module 04 외부 강의 링크 |
| `707a329` | Module 04 교재 페이지(making-of 8강) |

---

## 현재 배포 상태
- Git push → Vercel 자동 빌드 (커밋 메시지에 "배포" 포함 시에만 빌드 — Ignored Build Step)
- 캐시 버스팅: `script.js?v=...` (현재 `n`), `style.css?v=...`
- 배포 URL: `https://sunshine-wellness.vercel.app`
- 도메인: `sunshinewellness.co.kr` (GitHub Pages) — ⚠️ 최신 반영 지연/동기화 점검 필요

## 미완료 / 다음 작업
- /learn 모듈 본문: **관광 자원 · 해설** (docs 리서치 기반 초안 → 사용자 검수)
- /learn 다국어(현재 한국어만) — 반응 보고 EN/ZH 추가
- 도메인(sunshinewellness.co.kr) 최신 배포 반영 확인
- 사업자등록번호 등 실제 번호 입력(기존 미완)
