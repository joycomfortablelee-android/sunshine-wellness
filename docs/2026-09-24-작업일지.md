# 2026-09-24 작업일지

## **아침 — 주간 운영 페이지의 뼈대를 바꾸고, 색으로 「여는 곳」과 「받는 곳」을 갈랐다**

커밋 **스물넷**(10:36 `e3d4ba2` ~ 11:49 `cd99a04`). 이 창이 한 일은 셋이다 — ① **주간 운영 페이지**의 시간·순서·달력, ② **5·6주차 연구모형 자료**, ③ **알약과 링크의 색 규칙**. 나머지는 다른 창과 번갈아 올라갔다.

---

## 1. **수요일을 50분 + 50분으로** (`e3d4ba2` · `864bb39`)

먼저 표기만 바꿨다 — 「수요일 100분」을 **「50분 + 50분」**으로. 두 교시라는 사실이 글자에 드러나지 않던 것이다.

그다음 교수님이 **「준비 55분 · 발표 55분으로 할까」**를 물었다. 권하지 않았다 — **두 교시가 각 50분이라 110분이 나오지 않는다.** 쉬는 시간까지 합해야 그 길이가 된다. 대신 100분을 반씩 나누는 쪽을 제안했고 그대로 정해졌다.

| | 전 | 후 |
|---|---|---|
| 발표 | 60분 (3팀 × 20) | **50분** (17 · 17 · 16) |
| 제작 실습 | 40분 | **50분** |

단위표의 구성·합계 줄과 맨 위 부제까지 같이 고쳤다. *숫자를 한 곳만 고치면 표가 서로 다른 말을 한다.*

## 2. **절 순서를 바꿨다 — 발표 배정이 위로** (`d378a80`)

교수님이 **주간 운영표(3절) · 논문 세트(4절) · 단위표(5절) · 전체 보기(6절)** 넷을 묶어 **학생 발표 배정 아래로** 내리라 했다. 배정표가 3절이 되고 나머지가 뒤따른다.

1 달력 · 2 실제 진행 기록 · **3 발표 배정** · 4 운영표 · 5 논문 세트 · 6 단위표 · 7 전체 보기

- 번호는 등장 순서대로 다시 매겼다(정규식 한 번). **`#record` 같은 id 는 그대로**라 「2절 기록」을 가리키는 문구는 손댈 필요가 없었다.
- *절 번호는 글자이고 id 는 주소다. 순서를 바꿀 때 무엇이 깨지는지는 id 쪽만 보면 된다.*

## 3. **달력을 한 줄에 두 달씩** (`f022a9c`)

`auto-fit minmax(310px)` 이라 넓은 화면에서 **석 달이 한 줄**에 들어가 칸이 좁았다. `repeat(2, minmax(0,1fr))` 로 고정 — **9·10월 / 11·12월**. 칸이 넓어져 「도시 발표 1차 · 공모전 양식」 같은 메모가 덜 끊긴다. 700px 아래에서는 예전처럼 한 달씩 내려간다.

## 4. **5·6주차 연구모형 — 그림과 정리 노트** (`b72877d`)

바탕화면·내려받기 폴더에 있던 둘을 사이트로 들였다.

| 원본 | 사이트 | 크기 |
|---|---|---|
| `ETAM_UTAUT_AI_보완적이용의도_연구모형.png` | `learn/images/etam-utaut-ai-complementary-model.webp` | 223KB → **93KB** (가로 1200) |
| `…채팅답변_통합본.docx` | `learn/assets/etam-utaut-ai-complementary-use-notes.docx` | 255KB |

- 자리는 **강의계획서 웹판 주차표 바로 아래**(`#etam`). 설명을 주차와 이었다 — 그림 ①의 **TAM · 확장 TAM 이 5주차**, **UTAUT2 가 6주차**, 거기에 AI 리터러시 · 지각된 위험을 얹은 것이 ②다.
- 결과 쪽 **AI 보완적 이용의도**는 「AI 를 믿는가」가 아니라 **AI 와 사람의 강점을 어떻게 나눌 것인가**를 묻는다고 적어, 6주차 **혁신저항 · 후기수용모형(PAM)** 과 나란히 읽게 했다.
- alt 에 세 묶음의 구성 요소를 모두 적었다. 그 뒤 다른 창이 이 그림을 **접기**로 바꿨다(`f5f6945`) — 기본은 접힘, `#etam` 으로 들어오면 펼쳐진다.

## 5. **색으로 갈랐다 — 여는 것과 받는 것** (`862e1fa` · `845a36c` · `fdfcbc9`)

교수님이 **「웹에서 바로 보기」 알약의 색을 달리하면?」** 이라고 물은 데서 시작해 규칙이 하나 생겼다.

> **받는 것(DOCX · PPTX)은 그 칸의 색으로 채우고, 웹에서 여는 것은 따로 세운다.**

그다음 요청은 **「초록 말고 배경색과 보색으로」** 였다. 흐름 지도 두 칸의 바탕색을 재서 맞은편 색을 골랐다.

| 칸 | 바탕 | 알약 | 왜 |
|---|---|---|---|
| 파트 05 · 06 이론 | 올리브 `#7a6417` (47°) | **남보라 `#2c3a9e`** (227°) | 정확히 맞은편 |
| 파트 07 · 08 연구 | 보라 `#4d3f6e` (265°) | **금빛 `#d9a520`** (45°) | 정확히 맞은편 |

- *보색은 눈대중이 아니라 색상환 각도로 고른다 — 바탕의 HSL 을 재고 180° 를 더하면 된다.*
- 그 뒤 다른 창이 **두 알약을 모두 보라로 통일**하고(`ba3003b` · `0129c3a`) 글자를 **「15주 논문 읽기 계획」 · 「스마트관광 핵심 구조」**로 고쳤다(`cd99a04`). 색 규칙은 남고 표현만 더 구체적이 됐다.

## 6. **링크 하나가 세 번 바뀌었다** (`84a8b62` · `fe4aa6b`)

「주간 수업 운영 · 학생 발표 배정 — 1~15주차 한 장」 링크다.

**빨간 알약 → (교수님 정정) 빨간 줄글 → (다른 창) 초록 밑줄 → 파란 형광펜**

마지막 모양은 밑줄 한 가닥이 아니라 **글자 아래를 칠하는 형광펜**이다.

```css
background: linear-gradient(transparent 58%, #9fd0f5 58%, #9fd0f5 94%, transparent 94%);
```

> **처음에 안 먹었다.** 규칙을 `.learn-article .syl-box` 아래에 썼는데 이 링크는 **히어로(`.learn-hero`)** 안에 있었다. 선택자를 한 줄 더해 고쳤다(`fe4aa6b`). *같은 이름의 상자가 히어로와 본문 양쪽에 있는 페이지다 — 어느 쪽에 있는지 먼저 본다.*

## 7. **잔손질 둘**

- **「왼쪽 아래 칸」 삭제**(`e8d3003`) — 해커톤 공고문 설명에서 그림 속 위치를 가리키던 말을 뺐다. **공고문 판형이 바뀌면 틀린 안내가 된다.** 이제 「학생은 Section 1(대학생 · 대학원생)입니다」만 남았다.
- 어제 밤 고친 **출석부 우선순위**(`71a3f09`, 9/23 21:14)의 효과를 오늘 확인했다 — 페이지에 적어 둔 회차가 브라우저에 눌러 둔 값보다 앞선다.

## 8. **형광펜을 두 번 더 손봤다** (`d0912c5` · `62eaf6d`)

6번에서 만든 파란 형광펜이 두 걸음 더 갔다.

| 요청 | 한 것 |
|---|---|
| 「하이라이트색깔펜으로 글자를 덮는 파란색」 | 칠을 **글자 전체 높이**로(6%~96%) 올리고 색을 `#8fc9f4` 로 |
| 「색깔을 좀 연하게 · 클릭하고 싶게 특수 기호를」 | **`#d6ecfb`** 로 두 단계 낮추고 글자 앞에 **▶** 를 붙임 |

- ▶ 는 마우스를 올리면 **오른쪽으로 3px 밀리고** 색이 짙어진다. 끝의 ↗(새 탭)와 짝이 되어 「눌러서 다른 곳으로 간다」가 두 번 말해진다.
- *칠을 진하게 하면 덮는 느낌은 강해지지만 글자가 묻힌다. 덮개는 넓히고 색은 낮추는 쪽이 둘 다 산다.*

## 9. **다른 창이 같은 시간에 한 것**(참고)

`187e02c` 플랫폼 상자에 호스팅 여덟 칸 만화 · `6e020fa` 파트 06 아래 기술수용 이론 만화 네 장(TAM · TRAM · UTAUT2 · PAM) · `2afb3e5` · `b917a03` 척도개발 복습 알약 자리 정리 · `49c7266` 해커톤 포스터를 관광AI 포스터와 같은 배치로 · `8d04d69` · `4b78f01` · `51d0c5a` 문장 줄이기 셋 · `62d9bb5` AX 「왜 이 수업과 맞나」 붉게 · `628f77f` 주간 운영 제목 줄 초록 밑줄.

---

## 남은 것

- **1주차가 9/1(화)인지 9/2(수)인지** — 기록 표는 교수님 말씀대로 9/1, 달력은 9/2 칸. 한쪽으로 맞춰야 한다.
- **실습 1 · 실습 2 이름이 겹친다** — 안동 콘텐츠 제작 두 상자에 단 알약이 학기 과제(도시 카드 · 유럽 발표)와 같은 이름이다. 「제작 ① · ②」 같은 다른 이름을 물어 둔 상태.
- **9/23 발표 세 학생의 도시**(박상준 · 김윤서 · 김준호)와 **김대호의 9/9 칸**.
- **팀별 시간 17 · 17 · 16** — 실제로 해 보고 16 · 16 · 16 에 여유 2분이 나은지 본다.
- **같은 파일을 두 창에서 고치는 일이 오늘도 이어졌다** — 13:10 에는 다른 창이 만들던 **흐름 지도 파트 번호 알약**(CSS + 마크업)이 커밋되지 않은 채 남아 있어, 짝이 맞는지 확인하고 이 창이 올렸다(`f7f5784`). 곧이어 그쪽도 같은 내용을 자기 커밋으로 올렸다(`9d82ebb`). *한 페이지는 한 창에서 만지는 것이 커밋 기록을 읽을 수 있게 한다.*
- **파트 08 자료실로 가는 알약** — 지금 연구 칸 알약은 「스마트관광 핵심 구조」 표로 간다. 자료실 쪽이 맞으면 주소만 바꾸면 된다.

---

## 덧붙임 · 13시 이후

- `62b2fd6` 강의실 바 파트 줄에 **「15주 논문 읽기 계획 ↗」** 노란 알약(다른 창)
- `f7f5784` · `9d82ebb` **흐름 지도 파트 번호를 흰 알약**으로 — 누르면 그 파트로 내려간다. 파트 머리글마다 **01~08 이동 줄**도 함께 생겼다.

---

## 덧붙임 · 18시 이후 — 학생 발표 강의노트가 다섯 겹으로 쌓였다

커밋 **넷**(18:31 `dfe16e0` ~ 18:51 `6225151`). 파트 01의 「선정 시기·재원 구조」 상자 아래에, 학생들이 발표한 12개 도시를 다시 읽는 자료 다섯 개를 순서대로 쌓았다.

### 1. **12곳 강의노트를 새 페이지로** (`dfe16e0`)

7가지 질문(**WHY·WHERE·PLATFORM·SMART·EXPERIENCE·VALUE·LIMIT**)을 공통 틀로 삼아, 학생이 발표한 열두 도시를 16장 좌우넘김으로 정리했다. 마지막 장은 12곳 한눈에 비교표 + 비교 질문 넷.

### 2. **LMS 제출 목록으로 발표자를 채웠다** (`e21837a`)

교수님이 LMS 과제 게시판 목록(제출자 · 도시 · 제출 시각)을 통째로 붙여넣었다. 3페이지 표(「발표자마다 무엇을 다르게 보았나」)에 **발표자** 열을 새로 넣었는데, 목록만으로는 안 풀리는 자리가 둘 있었다.

- 학생은 보통 **두 번**(9/9 한 번, 9/16 한 번) 발표한 패턴이라, 이미 다른 도시에 배정된 학생이 같은 도시에 또 걸리면 어느 쪽이 맞는지 목록만으로는 알 수 없다.
- **경주**(전승예 · 서효원 제출 둘 다 있음), **양양**(정연승 · 김준호 둘 다 있음) — 교수님께 물어 **둘 다 표기**로 확정. **남원**은 애초에 「학생 PPT 미첨부」 행이라 발표자 없음(`—`)이 맞았다.
- *제출 목록은 「누가 냈나」이지 「이 표의 어느 칸인가」가 아니다 — 겹치는 이름이 있으면 목록만 읽고 채우지 않는다.*

같은 커밋에서 파트 01 링크 이름도 **「무엇을 만들었고 무엇이 남았나 — 12곳 학생 발표 강의노트」→「2026년 2학기 학생12도시 발표 강의노트」**로 바꿨다.

### 3. **내려받기 폴더의 대용량 슬라이드 셋 + 비교형 DOCX** (`bf991b6` · `6225151`)

「~html도 반영하자」가 연달아 왔다. 파일명만 받고 실제 파일은 **내려받기 폴더**에 있어, 매번 먼저 찾아야 했다.

| 요청한 이름 | 실제 위치 | 사이트 이름 | 크기 |
|---|---|---|---|
| 스마트관광도시_전체개요_포함_8장_좌우넘김 | 내려받기 폴더 (동일 파일 2벌) | `smart-tourism-8-sections-overview.html` | 27MB |
| 스마트관광도시_7개관점_만화_좌우넘김 (1) | 내려받기 폴더 (동일 파일 2벌) | `smart-tourism-7-perspectives-comic.html` | 22MB |
| 스마트관광도시_7개관점_그림_프롬프트_좌우넘김 | 내려받기 폴더 | `smart-tourism-7-perspectives-visual-prompts.html` | 47MB |
| 국내_스마트관광도시_12곳_비교형_강의노트 (2) word | 내려받기 폴더 (`(1)`, 접미사 없음 두 벌) | `learn/assets/smart-tourism-12-cities-comparison-note.docx` | 58KB |

- 파일 안에 이미지가 base64 로 박혀 있어 큼직하다. 올리기 전에 `learn/*.html` 중 이미 **20MB 짜리(`startup-business-planning-108p.html`)** 가 있는 걸 확인하고서야 그대로 올렸다 — *이 사이트는 큰 슬라이드 html 을 통으로 올리는 게 이미 관행이다. 매번 압축·분리를 새로 고민할 필요는 없다.*
- 파일명에 붙은 `(1)`·`(2)`·`(3)` 은 재다운로드 흔적일 뿐이었다 — `diff` 로 접미사 없는 파일과 동일함을 확인하고 접미사 없는 쪽을 썼다.
- 「알약 형태가 아니라」는 요청대로, 파트 흐름 지도의 흰 알약이 아니라 **12곳 강의노트와 같은 상자형**(`<p class="dltags">` + `<span class="dl-src">` 설명)으로 넣었다. 12곳 강의노트 바로 아래에서부터 순서대로: **강의노트(+DOCX) → 8장 전체개요 → 7관점 만화 → 7관점 그림+프롬프트**.
- 비교형 DOCX 는 별도 상자를 만들지 않고, 12곳 강의노트 상자 안에 **같은 내용의 다른 형식**으로 두 번째 링크(⤓ DOCX)로 붙였다 — 재원 구조 페이지가 이미 쓰던 「hot 링크 + 다운로드 링크 한 줄」 패턴을 그대로 따랐다.

### 남은 것

- **스마트관광도시_7개관점_그림_좌우넘김**, **7개관점_프롬프트**(텍스트만) — 내려받기 폴더에 아직 남아 있음. 반영 여부는 다음 지시 기다리는 중.

