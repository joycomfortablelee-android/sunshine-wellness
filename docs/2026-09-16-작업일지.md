# 2026-09-16 작업일지

## **아침 — 07장에 호텔 PMS · 클라우드 SaaS 두 판, 01장 신청유형 넷 그림**

오전 커밋 둘(11:23 `31a3ce9` · 11:27 `b5db4e3`). **AI 배리어프리 공고가 오늘 11:00 마감**이라 세션을 열자마자 먼저 알렸다 — 이미 지난 뒤였다.

```
① 어제까지의 진행 보고 · 마감 대조
② 07장 ONDA 옆에 호텔 PMS 판 · 클라우드 SaaS 아홉 장 판
⑤ OTA 결제방식 판에 익스피디아 두 모델 그림(다섯 장) · 98장 알약 노란색 되살리기
③ 01장 신청유형 넷을 한 장 그림(SVG)으로
④ 02장 세분화 알약 둘이 무엇이 다른지 설명(합칠지는 사용자 판단 대기)
```

---

## 1. **마감 대조 — AI 배리어프리는 오늘 11:00이었다**

세션 시작이 **11:07**. 메모리 `contest-deadlines` 의 「AI 지역관광 문제해결 · AI 배리어프리 — 9/16(수) 11:00」은 **7분 전에 지났다.**
경북 AX(9/15 18:00)도 어제 마감됐다. 둘 다 `learn/` 페이지에서 「내년 대비」로 내릴지 사용자 답을 기다린다.
남은 것은 **부산 Big Data 활용 대회 9/18(금) 17:00**, **데이터랩 경진대회 9/30**.

> **교훈 — 오전 마감은 「당일 알림」으로 늦다.** 11:00 마감은 전날 저녁에 알려야 했다.

## 2. **07장 ONDA 옆에 판 둘** (`31a3ce9`)

사용자가 Downloads 의 HTML 둘을 주며 「**온다 옆에 넣어줘**」.

| 받은 파일 | 올린 이름 | 무엇 |
|---|---|---|
| `hotel_pms_erp_summary.html` (16KB) | `learn/hotel-pms-system.html` | 호텔의 ERP? → **PMS** 이해. 여덟 절 한 쪽 |
| `클라우드_기반_SaaS_IaaS_PaaS_사례포함_수업용.html` (33KB) | `learn/cloud-saas-iaas-paas-slides.html` | **아홉 장 좌우 넘김**(← → 키 · 점 · 버튼) |

- 알약은 **인바운드 줄 ONDA 바로 뒤**, 주황(`mapl form hot`). 줄은 이제 **인바운드 18섹션 → 기업 다섯 → ONDA → PMS → 클라우드 → OTA 결제방식 → Booking.com** 일곱이다.
- 설명은 ONDA 한 문장 뒤에 이어 붙였다 — 「**그 B2B 인프라가 호텔 안에서 무엇과 연결되는지**를 보는 판 둘」로 잇고, 클라우드 판 끝의 **지역 체험사업자용 예약·정산 SaaS** 칸을 **2-2 사업모델** 칸과 이었다.
- **말투를 학생 쪽으로 고쳤다**(9/15 규칙대로 묻지 않았다) — PMS 판 넷(「수업용 한 문장」 · 「강의에서는」 · 「풀어 설명하는」 · 「07. 수업에서 이렇게 설명하면」), 클라우드 판 여섯(「학생들에게는 … 묻게 하면」 · 「수업 핵심」 · 「수업 질문」 ×3 · 「관광수업 포인트」).
- **링크 14개를 전부 열었다** — 12곳 200, **403 둘**(Cloudflare 학습문서 · Cloudbeds 헬프센터). 둘 다 **봇 차단**이지 죽은 주소가 아니라 그대로 두었다(브라우저로는 열린다).
- 슬라이드 스크립트 `node --check` 통과, 장 수 **9**. 헤더의 「1 / 7」은 표시용 초깃값이고 JS 가 9로 덮는다.

### 이 판들이 왜 07장에 맞나
PMS 판에 **OTA → 채널 매니저 → PMS → 현장 운영 → POS·RMS·CRM** 구조가 있어, 옆의 **OTA 결제방식**(누가 돈을 먼저 받나)과 **ONDA**(B2B 인프라)가 한 줄에서 이어진다.
**OCC · ADR · RevPAR** 계산식도 있어 숙박 아이템의 매출 추정에 그대로 쓴다.

## 3. **01장 신청유형 넷 — 한 장 그림** (`b5db4e3`)

9/15 「남은 것」의 미결(**네 칸 정리를 더 할지**)에 사용자가 **「네 칸에 대해서도 그림으로 넣어줘」**로 답했다.

- `learn/images/pre-tourism-venture-application-types-four.svg` — **2×2 카드**(번호 · 유형명 · 두 줄 풀이 · 예시 셋 · 「→ …쪽」 한 줄), 아래 **고르는 법** 한 줄.
- 자리는 **9/15 에 넣은 「일반현황」 그림 바로 아래**. 같은 표의 「신청유형(택 1)」 칸을 펼친 그림이라 붙여 두었다.
- 크롬 헤드리스로 렌더해 눈으로 확인했다(글자 넘침 · 겹침 없음). SVG 라 파일 6KB.

### 정의를 어디서 가져왔나 — **공고문 원문은 못 찾았다**

9/15 에 적어 둔 주의(「정의를 **공고문 원문에서** 확인할 것」)대로 원문을 찾았다.

| 열어 본 것 | 결과 |
|---|---|
| 바탕화면 `[붙임] 제18회 예비관광벤처부문 사업계획서 양식 (3).docx` | **이름 넷만** 있고 정의 없음 |
| 바탕화면 `touraz.jpeg` · `예비_성장_도전.png`(제18회 공식 포스터) | **부문 셋**(예비 15 · 초기 25 · 성장 25)뿐, 유형 넷 없음 |
| 투어라즈 공고 페이지 · bizinfo · 벤처스퀘어 · 트래블데일리 | 이름만 나열, 정의는 **첨부 공고문 PDF 안** |
| 검색 요약 | 정의 넷을 내놓았지만 **원문 대조가 안 됐다** — 근거로 쓰지 않았다 |
| `learn/assets/busan-tourism-startup-2026-guidelines.pdf` **2쪽** | **모집유형 표(개념 · 예시)가 원문 그대로 있다** |

그래서 **부산 모집요강의 유형 표를 바탕으로** 풀이와 예시를 적고, **캡션에 그 사실을 밝혔다** — 「유형 이름은 양식 그대로, 풀이·예시는 **수업용**이며 부산 모집요강을 참고했다. **관광벤처 공고문 원문의 정의와 글자까지 같지는 않다**」.
부산 공모는 유형 이름이 다르다(스마트관광 · 관광데이터·운영테크 · 관광인프라·공간혁신 · 체험·테마관광 · 지역특화) — **뜻이 겹치는 쪽끼리 옮겨 적은 것**이다.

> **교훈 — 원문을 못 구하면 「무엇을 근거로 썼는지」를 그림 옆에 적는다.** 검색 요약이 그럴듯해도 근거로 쓰지 않는다(9/11 산세바스티안 교훈).

## 4. **02장 세분화 알약 둘 — 무엇이 다른가** (설명만, 아직 손대지 않음)

사용자가 「이건 뭔지 설명해줘」라 해 둘을 나란히 열었다. **글 내용은 같다**(01 기본 관점 · 02 여섯 기준 · 03 관광 특화 · 04 통합 프롬프트 · 05 STP).

| | `startup-segmentation-prompts.html` | `startup-segmentation-6criteria.html` |
|---|---|---|
| 크기 | **15KB** | **2.6MB**(그림 하나가 base64) |
| 만든 경위 | 사용자가 준 **ChatGPT 답 글**을 페이지로 지음 | 사용자가 준 **디자인판 HTML** 그대로 |
| 쓰임 | **작업판** — 프롬프트마다 **복사 버튼**, 바꿔 넣을 칸 노란 글자 | **정리판** — 한 화면에 쭉, 인쇄에 맞음 |

합칠지는 사용자 판단으로 남긴다. 지우면 링크 하나가 사라지고, 두면 같은 글이 두 알약에 있다.

## 5. **OTA 결제방식 판에 익스피디아 두 모델 그림 — 다섯 장이 됐다** (`f31337c`)

사용자: 「ChatGPT Image … 11_34_43 · 이거 호텔 관련 html 에 넣는다면?」 그림을 열어 보니 **익스피디아의 숙박 예약 수익구조 비교**
(Expedia Collect = 머천트 · Hotel Collect = 에이전시, 「누가 먼저 돈을 받는가」 + 3행 비교표)였다. 자리 셋을 올렸고 사용자가 **1번**을 골랐다.

| 순위 | 자리 | 근거 |
|---|---|---|
| **1(채택)** | `ota-payment-models-slides.html` 의 **2장으로 끼우기** | 그 판이 **1장 개념 → 2장 Expedia 공시 수치**로 건너뛰던 **빠진 계단**. 알약은 늘지 않는다 |
| 2 | Booking.com SEC 증거 판 옆 새 알약 | 대조는 선명해지나 줄이 여덟 알약 |
| 3 | 오늘 올린 PMS 판 안 | PMS 는 호텔 **내부 운영** 얘기라 결제 모델은 곁가지 |

- PNG **1.36MB → webp 174KB**(원 해상도 1448×1086 유지, q90) `learn/images/ota-payment-models-expedia-collect.webp`. 9/15 규칙 그대로.
- 탭 · 점 · 쪽번호 · `const total` 을 **4 → 5**로 고치고 기존 2~4장을 3~5장으로 밀었다. 장 이름은 **2. Expedia 두 모델 / 3. Expedia 수치**로 갈랐다.
- 07장 알약 글자를 「네 장」 → 「**다섯 장**」으로, 설명에 **Expedia Collect · Hotel Collect** 한 구절을 넣었다.
- 헤드리스로 **새 2장을 띄워 렌더 확인**했다(탭이 두 줄로 접히지만 그림 칸은 그대로). 넘김 버튼은 눌러 보지 않았다.
- 그림 안 「수업용 표현: 판매마진형 구조」처럼 **공식 분류가 아닌 설명**이 섞여 있으나, 그림에 이미 「수업용 표현」이라 적혀 있어 그대로 두었다.

## 6. **98장 알약이 노란색이 아니었다 — CSS 우선순위** (`f31337c`)

사용자: 「사업계획서 합격 비법 · 98장 알약을 노란색으로」. 열어 보니 **9/15 에 이미 노란 규칙을 넣어 두었는데 색이 안 먹고 있었다.**

```
.learn-article .q-cell .linkrow a.ylw98            → 클래스 4 (0,4,1)   ← 졌다
.learn-article .quad.learnq .linkrow.tiny a        → 클래스 5 (0,5,1)   ← 이겼다(파란 기본 알약)
```

선택자를 `.learn-article .quad.learnq .linkrow.tiny a.ylw98` (0,6,1) 로 올려 해결했다. 색값(`#ffe800` · 테두리 `#d8bb2a` · 글자 `#14120a`)은 그대로다.
그 칸만 떼어 낸 임시 페이지를 헤드리스로 찍어 **노랗게 나오는 것을 눈으로 확인**했다.

> **교훈 — 「규칙을 넣었다」와 「색이 먹는다」는 다르다.** 같은 요소에 이미 걸린 규칙의 **클래스 개수를 세어** 보고, 새 규칙은 그보다 길게 쓴다. 색을 바꾼 뒤에는 **그 부분만 잘라 찍어 본다.**

---

---

## 오늘의 규칙

- **오전 마감은 전날 저녁에 알린다.**
- **받은 판의 링크는 전부 열되, 403 은 「봇 차단」인지 「죽은 주소」인지 갈라 본다** — 봇 차단이면 그대로 둔다.
- **공고문 원문을 못 구하면 그림에 「무엇을 근거로 한 풀이인지」를 적는다.** 검색 요약은 근거가 아니다.
- **색이 안 먹으면 규칙을 또 쓰지 말고 이긴 규칙의 클래스 수를 센다.** 고친 뒤에는 그 부분만 잘라 찍어 확인한다.
- **좌우 넘김 판에 장을 끼울 때는 다섯 곳을 같이 고친다** — 쪽번호 · 탭 · 점 · `data-index` · `const total`.

---

## 남은 것

- ~~AI 배리어프리(9/16 11:00) · 경북 AX(9/15 18:00) 마감 지남 — 내릴지 답 대기~~ → **2026-09-16 종결. 사용자 「내버려둔다」** — 페이지의 두 공고는 **그대로 둔다.** 「내년 대비」 배지도 달지 않는다. 앞으로 마감이 지나도 **알리기만 하고 페이지는 건드리지 않는다.**
- ⚠️ **부산 빅데이터 활용 대회 — 9/18(금) 17:00.**
- **신청유형 넷 그림** — 공고문 PDF 를 구하면 풀이를 **원문 정의로 교체**할 것.
- ~~02장 세분화 알약 둘 — 합칠지 답 대기~~ → **2026-09-16 종결. 사용자 「내버려둔다 · 현재 상태 유지」** — 작업판(복사 버튼)과 한 장 정리판을 **둘 다 그대로 둔다.**
- **9/15 마지막 커밋 `df6b15c`(사업계획서 합격 비법 98장, 21:35)** 은 9/15 작업일지에 안 적혔다.
- 9/15 「남은 것」의 나머지(복사 버튼 클릭 확인 · 무거운 판 셋 휴대폰 로딩 · OTA 수치 원문 대조 · 창업정보 34선 말투와 DIGIECO · 타겟팅 알약 색 · 경북 관광 스타트업 공모 마감일)는 그대로 유효하다.
