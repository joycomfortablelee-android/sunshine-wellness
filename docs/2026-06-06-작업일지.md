# 2026-06-06 작업일지

## Where to Next?(다음 여행지) — Visit Busan AI 안내 카드 위치 조정 + 저장소 정리

> ⚠️ Vercel은 **커밋 메시지에 "배포"가 있을 때만 빌드**(Ignored Build Step). 없으면 `Canceled` → `vercel --prod --yes` 강제 배포.

---

## 1. Visit Busan AI 카드 — 섹션 상단 → 본문 하단으로 이동 (`559800c`)

### 배경
- 직전(`63e52ca`)에 `Where to Next?`(다음 여행지) 게시판 **상단**(Community 라벨 위)에 "부산시 공식 AI 여행 추천" 안내 카드(`.vb-aic`)를 추가했었음.
- 그 위치가 어색하다는 피드백 → 게시판 **본문 내용의 가장 아래**(목록·검색창 다음)로 이동 요청.

### 변경 (`script.js` — `openWhereToNextPage()`)
- `.vb-aic` 블록을 `.sp-wrap` 최상단에서 **검색창(`.sp-search`) 바로 다음, `.sp-wrap` 닫기 직전**으로 이동.
- 페이지 표시 순서: `배너 → Community → 다음 여행지 → 게시판 목록 → 페이징 → 검색 → Visit Busan AI 카드`.
- 링크(`visitbusan.net …menuCd=DOM_000000203018000000`)·라벨(KO/EN/ZH `labels.aicTitle/aicDesc/aicBtn`)은 그대로 유지.

### 간격 보정 (`style.css` — `.vb-aic`)
- 카드가 상단에 있을 땐 `margin-bottom:24px`만 있어, 하단으로 옮기니 **검색창에 바로 붙음**.
- `.vb-aic`에 **`margin-top:28px` 추가** → 검색창과 카드 사이 간격 확보.

---

## 2. 저장소 정리 — `.claude/` 추적 해제

- 직전 커밋(`559800c`)에 Claude 작업용 임시 파일 `.claude/scheduled_tasks.lock`이 함께 올라가 있었음.
- `.gitignore`에 **`.claude/` 추가** + `git rm -r --cached .claude/`로 추적 해제(로컬 파일은 유지). 이후 `.claude/` 변경이 git에 잡히지 않음.

---

## 커밋 이력 (2026-06-06)

| 커밋 | 내용 |
|---|---|
| `559800c` | Visit Busan AI 카드 게시판 하단 이동 + 검색창과 간격(margin-top) 추가 |
| (예정) | `.gitignore`에 `.claude/` 추가 + 추적 해제 + 작업일지(06-06) |

---

## 현재 배포 상태
- 배포: `https://sunshine-wellness.vercel.app` / 도메인 `sunshinewellness.co.kr`(GitHub Pages) — `/api`는 Vercel에서만 실행
- `559800c` 푸시 완료 → 자동 배포 반영

## 미완료 / 다음 작업 (06-05에서 이월)
- **🔴 Module 08 「부산의 웰니스 자원」 검수** — 동래온천·금정산·이기대·범어사 템플스테이 등 실제 현황 확인 후 노란 "검수 요청" callout 제거
- (선택) Module 09 실습에 예시/완성 영상 링크 추가
- /learn 다국어(현재 한국어만) — EN/ZH 추가 검토
- 챗봇 아바타(💁‍♀️ 이모지)를 실제 AI 일러스트 이미지로 교체 검토
- 챗봇 창 `width:370px` → 아주 좁은 폰(≤390px) 대응 검토
- 다크 투어리즘 EN/ZH '여행 정보'에 부산근현대역사관 운영정보 동일 반영(현재 KO만)
- 챗봇 FAQ_DB 보강 / (선택) 진짜 LLM 상담 연동 검토
