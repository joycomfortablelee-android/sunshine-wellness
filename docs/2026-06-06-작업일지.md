# 2026-06-06 작업일지

## 교육자료 모듈 3·3·3 레벨 재배치(디지털 강조) + Where to Next 카드 정리

> ⚠️ Vercel은 **커밋 메시지에 "배포"가 있을 때만 빌드**(Ignored Build Step). 없으면 `Canceled` → `vercel --prod --yes` 강제 배포.

---

## 1. 교육자료(/learn) 모듈 3·3·3 레벨 재배치 (`fcf788c`) ⭐ 메인 작업

### 배경 — 경기대 관광문화콘텐츠학과 교원 지원
- 사용자가 **경기대학교 관광문화콘텐츠학과 교원**에 지원. 학과 교육과정(2025 시행세칙 + SW심화트랙) 분석: 학과 정체성 = **콘텐츠화 · MICE/이벤트 · 디지털 기술 융합**, 특히 ⭐**SW심화트랙 "디지털관광문화콘텐츠"**가 학과가 미는 방향.
- 전략: 학과가 이론 중심인 지점을 우리가 실제로 만들어 배포한 결과물(이 사이트)로 채우는 **디지털 제작 실습 역량**을 전면에 — **심사위원이 `/learn`에서 가장 먼저 보도록** 디지털 블록을 맨 위로.
- (신규 모듈 후보였던 "지역축제·이벤트 콘텐츠"는 학과 정규 교과 `지역축제콘텐츠(HE128)`에 이미 있어 **추가 안 함** → 재배치로 방향 전환.)

### 변경 — 3블록(레벨) 구조, 9개 = 3·3·3
| 레벨(그룹헤더) | 모듈(표시번호) | 파일(URL 불변) |
|---|---|---|
| ⭐ **디지털 관광 콘텐츠 제작** | 01 웹사이트 빌드 · 02 UX/UI · 03 영상 홍보 제작 실습 | making-of · terms · video-promotion |
| **관광의 기초** | 04 관광 자원 · 05 문화공간 재생 · 06 관광 홍보론 | tourism-resources · 메인딥링크 · tourism-promotion |
| **실전 · 특화** | 07 해설 · 08 웰니스 관광 · 09 여행 영어 | interpretation · wellness-tourism · travel-english |

- 실제 이동: 영상(기존09→03)·웹빌드(05→01)·UX(06→02)를 맨 위 블록으로, **해설(기존02→07)을 실전·특화로 이동**해 3·3·3 균형, 여행영어(07→09).
- **파일명(URL) 고정, 표시번호만 재정렬** → SEO·기존 링크 안전.

### 제작물
| 파일 | 내용 |
|---|---|
| `learn/index.html` | 카드 3블록 재배치 + 그룹헤더 3개(`.learn-group`, `.is-key`=디지털) + Module 0X 재라벨, `learn.css?v=13` |
| `learn/learn.css` | `.learn-group`/`.gh-title`/`.gh-line`/`.gh-badge`/`.is-key` 그룹헤더 스타일 추가 |
| 모듈 8개 페이지 | 상단 라벨 `Module 0X` 재번호 + 이전/다음 `mod-nav` 전부 재연결 |

### 세부 — 본문 상호참조를 번호→모듈명으로
- 재번호 시 본문 속 `04모듈`·`다음 모듈 「웹사이트 빌드」`·`앞의 세 모듈` 같은 참조가 깨짐 → **모듈명 기준**("「관광 홍보론」 모듈", "「해설」 모듈")으로 변경해 **향후 재배치에도 안 깨지게** 함.
- 검증(grep): 라벨 9개 01~09 정확 매핑 ✅ / 잔여 `0X모듈` 참조 0건 ✅ / nav 체인 01→09 양방향 일관 ✅ (05 문화공간재생은 기존처럼 메인 딥링크 `../#cultural-archive`).

---

## 2. 교육자료 Open Design 참고 링크 (`7c3c7ff`)
- 허브/메이킹노트에 **"더 읽어보기 · References"**(`.learn-refs`) 추가 — Open Design(Anthropic Claude·Vercel 활용 AI 디자인 도구) 정리글 링크. LLM이 '디자인 아티팩트'를 직접 생성하는 흐름 소개.

---

## 3. Where to Next?(다음 여행지) — Visit Busan AI 안내 카드

### 3-1. 카드 추가 (`63e52ca`)
- `Where to Next?`(다음 여행지) 게시판 **상단**(Community 라벨 위)에 "부산시 공식 AI 여행 추천" 안내 카드(`.vb-aic`) 추가. 링크 `visitbusan.net …menuCd=DOM_000000203018000000`, 라벨 KO/EN/ZH(`labels.aicTitle/aicDesc/aicBtn`).

### 3-2. 상단 → 본문 하단으로 이동 (`559800c`)
- 상단 위치가 어색 → 게시판 **본문 가장 아래**(목록·검색창 다음)로 이동.
- `script.js`(`openWhereToNextPage()`): `.vb-aic`를 `.sp-wrap` 최상단 → 검색창(`.sp-search`) 바로 다음, `.sp-wrap` 닫기 직전으로 이동. 표시 순서: `배너 → Community → 다음 여행지 → 게시판 목록 → 페이징 → 검색 → Visit Busan AI 카드`.
- `style.css`(`.vb-aic`): 하단으로 옮기니 검색창에 붙음 → **`margin-top:28px` 추가**로 간격 확보.

---

## 4. 저장소 정리 — `.claude/` 추적 해제
- `559800c`에 Claude 작업용 임시 파일 `.claude/scheduled_tasks.lock`이 함께 올라가 있었음.
- `.gitignore`에 **`.claude/` 추가** + `git rm -r --cached .claude/`로 추적 해제(로컬 파일 유지). 이후 `.claude/` 변경이 git에 안 잡힘.

---

## 커밋 이력 (2026-06-06, docs 제외)

| 커밋 | 내용 |
|---|---|
| `63e52ca` | Where to Next? 상단 Visit Busan 공식 AI 추천 카드 추가 |
| `7c3c7ff` | 교육자료 Open Design 참고 링크(`.learn-refs`) 추가 |
| `fcf788c` | 교육자료 모듈 3·3·3 레벨 재배치(디지털 블록 맨 위) ⭐ |
| `559800c` | Visit Busan AI 카드 게시판 하단 이동 + 검색창 간격(margin-top) |

---

## 현재 배포 상태
- 교육자료 캐시: `learn.css?v=13`(허브)
- 배포: `https://sunshine-wellness.vercel.app` / 도메인 `sunshinewellness.co.kr`(GitHub Pages) — `/api`는 Vercel에서만 실행. `vercel --prod --yes` 강제 배포로 최신 반영(`status: ok`).
- 라이브: `https://www.sunshinewellness.co.kr/learn/` (디지털 블록 맨 위 3·3·3)

## 미완료 / 다음 작업 (06-05에서 이월)
- **🔴 Module 08 「부산의 웰니스 자원」 검수** — 동래온천·금정산·이기대·범어사 템플스테이 등 실제 운영·접근성·최신 현황 사용자 확인 → 확정 후 페이지 내 노란 "검수 요청" callout 제거
- (선택) 허브 그룹헤더 문구/강조 강도 조정 — 심사위원 인상 보고
- (선택) Module 09 실습에 예시/완성 영상 링크 추가
- /learn 다국어(현재 한국어만) — EN/ZH 추가 검토
- 챗봇 아바타(💁‍♀️ 이모지)를 실제 AI 일러스트 이미지로 교체 검토
- 챗봇 창 `width:370px` → 아주 좁은 폰(≤390px) 대응 검토
- 다크 투어리즘 EN/ZH '여행 정보'에 부산근현대역사관 운영정보 동일 반영(현재 KO만)
- 챗봇 FAQ_DB 보강 / (선택) 진짜 LLM 상담 연동 검토
