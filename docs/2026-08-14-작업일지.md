# 2026-08-14 작업일지

## 스마트 투어리즘 최상단 카드 — 메타 줄 링크화 + 경희대 스마트관광연구소 연결

사용자 요청: *"한국관광공사 · korean.visitkorea.or.kr/smarttourcity 이거 링크 클릭하면 연결되도록, 그리고 옆에 https://strc.khu.ac.kr/01/02.php 이것도 연결"*

대상은 `learn/smart-tourism.html` 최상단의 파란 강조 카드(`li.hi`). 제목 줄은 이미 링크였지만, 그 아래 **메타 줄의 도메인 텍스트는 링크가 아니라 회색 글자**였다. 사용자가 그 줄을 눌러 봤다는 뜻이므로, 보이는 URL은 눌리는 게 맞다고 판단.

---

## 1. 메타 줄 링크화 (`learn/smart-tourism.html:45`)

**변경 전**
```html
<span class="meta">한국관광공사 · korean.visitkorea.or.kr/smarttourcity · "여행을 더 똑똑하게, 로컬을 더 생생하게"</span>
```

**변경 후** — 도메인을 제목과 같은 목적지로 연결하고(`↗`), 뒤에 「함께 보기」로 경희대 링크 추가.
```html
<span class="meta">한국관광공사 ·
  <a href="…/smarttourcity/main/index.kto" target="_blank" rel="noopener">korean.visitkorea.or.kr/smarttourcity ↗</a>
  · "여행을 더 똑똑하게, 로컬을 더 생생하게" · 함께 보기
  <a href="https://strc.khu.ac.kr/01/02.php" target="_blank" rel="noopener">경희대 스마트관광연구소 연혁 ↗</a>
</span>
```

### 링크 문구를 "연혁"으로 잡은 근거
`strc.khu.ac.kr/01/02.php` 는 URL만으로는 무슨 페이지인지 알 수 없어 **WebFetch로 실제 내용을 확인**한 뒤 문구를 정했다.

> 경희대학교 관광산업연구원 **스마트관광연구소** — `History` 페이지. 2013년 개소부터 2020년까지 연구 성과·교수진 활동·정부 과제 선정·국제 학술활동을 시간 역순으로 기록.

즉 이 링크는 **공식 플랫폼(현장)** 옆에 붙는 **학술·연구 계보**다. 카드의 성격(현장 먼저 보기)과 층위가 다르므로 제목 링크로 올리지 않고 메타 줄에 「함께 보기」로 배치.

---

## 2. CSS — 메타 줄 전용 링크 규칙 신설 (`learn/learn.css:115`)

`.meta` 안에 그냥 `<a>`를 넣으면 **자료실 링크 기본 규칙이 먼저 잡힌다**:

```css
.learn-article .reslist a { font-size: 16px; font-weight: 800; color: var(--deep); }
```

→ 12px 회색 메타 줄 한가운데에 **16px 굵은 제목 크기 글자**가 끼어들어 줄이 깨진다. `.desc a` 에 이미 같은 목적의 오버라이드가 있었으므로(`:114`) 같은 방식으로 `.meta a` 를 추가:

```css
.learn-article .reslist .meta a { font-size: inherit; font-weight: 700; color: inherit; border-bottom: 1px solid currentColor; }
.learn-article .reslist .meta a:hover { text-decoration: none; opacity: .78; }
```

- `color: inherit` — `li.hi .meta { color: #2563a8 }`(`:127`)를 그대로 물려받아 카드 파란색 유지. 색을 새로 박지 않아 강조/비강조 카드 양쪽에서 자동으로 맞는다.
- 링크임을 알리는 신호는 **밑줄 1px만** — 크기·색이 주변과 같으므로 밑줄이 유일한 단서다.

---

## 3. ⚠️ 캐시 버전 누락 — 커밋 후 발견해 후속 수정

첫 커밋(`3fd222c`)에서 **`learn.css` 를 수정하고도 `smart-tourism.html` 의 캐시 버전을 올리지 않았다.** 그 페이지는 `learn.css?v=20` 을 참조 중이었다.

**증상(재방문자 한정)**: HTML은 새로 받지만 CSS는 브라우저 캐시의 v=20 — `.meta a` 규칙이 없으므로 `.reslist a` 가 적용돼 **메타 줄의 두 링크가 16px 굵은 글씨로 튀어나온다.** 신규 방문자·강력새로고침에서는 정상이라 놓치기 쉬운 종류.

→ `learn.css?v=20` → **`v=24`** 로 상향(`learn/smart-tourism.html:14`).

### 현재 캐시 버전 현황 (불일치 상태)
| 파일 | 버전 |
|---|---|
| `smart-tourism.html` | **v=24** (오늘) |
| `careers.html` | v=23 (2026-08-08) |
| 나머지 12개 페이지 | v=20 |

이번 변경은 `.meta a` **추가 규칙뿐**이고 `.meta a` 를 쓰는 페이지가 현재 `smart-tourism.html` 하나이므로, 나머지 페이지를 함께 올리지 않아도 깨지는 곳은 없다. 다만 **버전이 세 갈래로 갈라진 상태**라, 다음에 공통 규칙을 건드릴 때는 13개 페이지를 한 번에 통일할 것.

> 교훈: **`learn.css` 를 수정했으면 그 규칙을 쓰는 페이지의 `?v=` 를 같은 커밋에서 올린다.** 08-08 세션에서 "CSS 변경 없음 → 버전 유지"를 여러 번 판단한 것의 반대 방향인데, 반대쪽을 빠뜨렸다.

---

## 4. 커밋

| 커밋 | 내용 | "배포" |
|---|---|---|
| `3fd222c` | 메타 줄 링크화 + 경희대 링크 + `.meta a` CSS | ✅ |
| (후속) | `learn.css?v=24` 캐시 버스팅 + 작업일지 | ✅ |

커밋 메시지에 **"배포"** 포함 — Vercel 빌드 정상 진행. ([[board-and-hosting]] 규칙)

---

## 5. 2026-08-08 세션의 미기록 커밋 2건 (뒤늦게 확인)

08-08 작업일지는 `21bd46f`(19:01)에 작성됐는데, 그 **뒤에 커밋 2건이 더 있었고 일지에 반영되지 않았다.**

- `3523ef3` (19:05) — `tourism-venture.html` 「관련 링크 · 창업 지원 사업」의 로컬크리에이터 6건에 **Ⓛ(U+24C1) 표시** + 읽는 순서 안내문(통합공고 → 모집공고 → 지역판 → 보도자료 → 세부관리기준 → 공식 블로그). 유니코드 문자만 사용 → CSS 무변경.
- `d0134a8` (19:12) — `careers.html` 경로 4(로컬 크리에이터)에 **같은 Ⓛ 링크 6건을 별도 `reslist`로 복제 배치**. 관광벤처 모듈까지 건너가지 않고 그 자리에서 공고를 열 수 있게 함. 기존 링크 목록 앞에는 "결과물을 만드는 재료와 일감" 구분 문단을 넣어 두 목록의 성격을 분리. 태그 균형 `li 97/97`, `ul 21/21`.

두 건 모두 "배포" 포함 커밋이라 반영 자체는 정상. **일지만 비어 있던 것**이므로 여기에 기록해 둔다.

---

## 6. (같은 날 세션 2) 관광벤처 「창업 지원 사업」에 링크 2건 추가

사용자 요청: *"관광 벤처 사업 부분에 (구글 드라이브 링크) 이것도 연결해줘, (publy.co/topic/237) 이것과 함께"*

대상은 `learn/tourism-venture.html` 「관련 링크 · 창업 지원 사업」 목록. 두 링크를 **CLASS101 사업계획서 강의 바로 뒤**에 나란히 배치했다 — 그 자리가 *공고를 찾는 자료*가 끝나고 *서류를 실제로 쓰는 도구*가 시작되는 지점이기 때문.

### 링크 정체 확인 (URL만으로는 알 수 없어 실제 확인)
| 링크 | 확인 방법 | 실제 정체 |
|---|---|---|
| Google Drive 폴더 | Drive MCP `get_file_metadata` + WebFetch | **「[부록] 정부지원사업 AI 자동화 키트」** — 소유 계정 `harry@newneek.co`, 2026-08-02 공유. 파일 4개: ①가이드 문서 ②실행 소스코드(zip) ③프롬프트북_정부지원사업_항목별초안.md ④가상심사위원_자가채점프롬프트.md |
| publy.co/topic/237 | WebFetch | PUBLY(퍼블리) **커리어·실무 콘텐츠 멤버십**의 「BEST」 목록(상단 네비 항목). 관광·창업 전용 콘텐츠는 아님 |

### 배치 근거
- **AI 키트** — ④ 「가상심사위원 자가채점」이 바로 위 항목인 **KISED 세부관리기준**(평가 기준·정산 규정 원문)과 정확히 짝을 이룬다. "기준을 읽는다 → 그 기준으로 내 서류를 채점해 본다"로 이어지므로, 설명문에서 세부관리기준을 명시적으로 되짚었다. 동시에 **"AI가 만든 것은 초안"** 이라는 경고를 넣어, 자격 요건·금액·마감일은 공고 원문 대조를 요구했다(기업마당 블로그 항목의 "요약은 요약일 뿐" 경고와 같은 톤).
- **PUBLY** — 관광 공고 창구가 아니므로 그 점을 설명문에 그대로 밝히고, 대신 *"시장 분석·고객 정의·실행 계획을 남들은 어떤 언어로 쓰는가"* 라는 용도로 한정했다. **유료 멤버십**임도 명시(CLASS101 항목의 `(유료)` 표기와 동일 원칙).

### URL 정리
사용자가 준 Drive 주소에 붙어 있던 **branch.io 추적 파라미터**(`$web_only`, `_branch_match_id`, `_branch_referrer`)와 PUBLY의 `?fr=top-navi` 를 **모두 제거**하고 정규 주소만 남겼다. 추적 파라미터는 링크를 준 사람의 유입 경로를 담고 있어 공개 페이지에 박을 이유가 없다.

CSS 변경 없음 → `?v=` 유지. 태그 균형 확인: `li 97/97`, `ul 18/18`, `span 72/72`.

### 후속 — 세 번째 링크로 앞의 두 건이 한 묶음이 됨 (`1a94dde`)

사용자가 이어서 `publy.co/content/8151` 을 추가 요청. 확인해 보니 **바로 앞 세션에서 넣은 드라이브 키트의 본문 글**이었다.

> **「정부지원사업, AI로 공고 스크리닝부터 사업계획서 초안 작성까지 끝내는 법」** — PUBLY '일잘러의 업무스킬', 저자 **신대리**(의료기기 스타트업에서 연평균 50억원 규모 지원사업 서류 작성). 내용 4가지: 회사 자료를 AI에 안전하게 넣는 법 / 심사 항목별 초안 프롬프트 / AI 가상 심사위원 사전 검증 / 맞춤 공고 자동 수집·슬랙 알림 코드. 유료(무료체험 제공).

→ **본문 → 부록(드라이브) → 플랫폼(BEST)** 순서가 되도록 키트 **바로 앞**에 삽입하고, 뒤쪽 두 항목의 첫 문장을 고쳐 관계를 명시했다.
- 키트: "사업계획서를 AI와 함께 쓰는…" → **"위 글에 딸린 부록 자료로, …"**
- BEST: "위 AI 키트와 함께 볼 만한…" → **"위 AI 글이 실려 있는 실무 콘텐츠 멤버십의 「BEST」 목록"**

본문 설명에는 이 절 첫머리의 *"공고는 찾는 사람에게만 보인다"* 를 되짚어, ④ 공고 자동 수집·슬랙 알림이 **그 '찾는 일' 자체를 매일 자동으로 돌리는 방법**임을 연결했다. 추적 파라미터 `?fr=library-done` 은 제거.

태그 균형 재확인: `li 98/98`, `span 74/74`.

| 커밋 | 내용 | "배포" |
|---|---|---|
| `1ed349a` | 드라이브 키트 + PUBLY BEST 2건 추가 | ✅ |
| `1a94dde` | PUBLY 본문 글 추가 + 앞뒤 항목 연결 문구 수정 | ✅ |

---

## 7. 남은 논점

- **`learn.css?v=` 3분열(20 / 23 / 24)** — 다음 공통 CSS 수정 시 13개 페이지 일괄 통일.
- 08-08 이월분 그대로 유지: `bizinfo1357` 블로그 글 제목 미확인 · 점핏/K-DATA 링크 거취 · 2026년 「소상공인 도약 지원 사업」 회차 금액 · `tourism-venture.html` 412줄 목차 검토 · 옛 앵커 `careers.html#freelance` 리다이렉트 여부.
